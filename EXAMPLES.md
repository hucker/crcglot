# crcglot generated code gallery

Every block below is real `crcglot` output.  Reproduce any of it with the command shown above the code.

Three tours, each varying one thing:

- [Every language](#every-language): what crcglot emits for each of the 11 targets, same algorithm and same defaults, so the only difference between blocks is the language.
- [Implementation variants](#implementation-variants): what `--small`, `--table`, and `--slice8` change, all in one language.
- [Documentation styles](#documentation-styles): what `--comment` changes, one block per style.

The language tour uses `crc16-ibm-sdlc` (IBM SDLC, ISO HDLC, X.25 FCS; width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF).  Matching a 16-bit CRC that some existing wire format already fixed is the job crcglot is for.  Every generated file embeds a `_self_test()` that checks itself against the catalogue's canonical value, `crc("123456789") == 0x906E`.

The variant tour uses `crc32-bzip2` instead, because slice-by-8 exists only at width 32 and 64.  It is also non-reflected, so its inner loop is the other of the two shapes the generators emit.

Want a different algorithm?  Substitute the name (`crcglot list` for the full catalogue) and re-run the command.

Every block is collapsed by default.  Click a heading to expand.

This file is auto-generated by `scripts/regenerate_examples.py`.  Do not hand-edit; re-run the script instead.

## Quick links

- **Every language**: [C / C++](#lang-c) · [C#](#lang-csharp) · [Go](#lang-go) · [Java](#lang-java) · [Lua](#lang-lua) · [Python](#lang-python) · [Rust](#lang-rust) · [TypeScript](#lang-typescript) · [Verilog](#lang-verilog) · [VHDL](#lang-vhdl) · [Zig](#lang-zig)
- **Implementation variants**: [Bit-by-bit](#variant-bitwise) · [Table-driven](#variant-table) · [Slice-by-8](#variant-slice8)
- **Documentation styles**: [Plain](#style-c-plain) · [Doxygen](#style-c-doxygen) · [DocFX](#style-csharp-docfx) · [Godoc](#style-go-godoc) · [Javadoc](#style-java-javadoc) · [Google](#style-python-google) · [NumPy](#style-python-numpy) · [reStructuredText](#style-python-rest) · [Rustdoc](#style-rust-rustdoc) · [JSDoc](#style-typescript-jsdoc)

## Every language

The same algorithm (`crc16-ibm-sdlc`), generated with no flags, for every target.  No flags means `--fast`, so each block is the fastest implementation that target supports at width 16.  Verilog and VHDL are bit-by-bit because that is the only form that makes sense in hardware.

<a id="lang-c"></a>

<details>
<summary>

### C / C++

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot c crc16-ibm-sdlc
```

**`crc16_ibm_sdlc.h`**

```c
/* crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16_ibm_sdlc(data, len) over "123456789" == 0x906E
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint16_t s = crc16_ibm_sdlc_init();
 *     s = crc16_ibm_sdlc_update(s, chunk, chunk_len);
 *     uint16_t crc = crc16_ibm_sdlc_finalize(s);
 *
 * One-shot: crc16_ibm_sdlc(data, len)
 * Verify:   crc16_ibm_sdlc_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    c
 *     variant:   table
 *     comment:   plain
 *     symbol:    crc16_ibm_sdlc
 *     naming:    snake
 */
#ifndef CRC16_IBM_SDLC_H
#define CRC16_IBM_SDLC_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Return the initial CRC state to begin a computation.
 *
 * Returns the starting uint16_t state.
 */
uint16_t crc16_ibm_sdlc_init(void);

/* Fold input into the running CRC state and return the new state.
 *
 * state: running uint16_t state (from init or a prior update).
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the updated uint16_t state (not yet finalized).
 *
 * Call init -> update (any number of times) -> finalize.
 */
uint16_t crc16_ibm_sdlc_update(uint16_t state, const uint8_t *data, size_t len);

/* Apply the final XOR to produce the CRC.
 *
 * state: accumulated uint16_t state from update.
 *
 * Returns the finished CRC value.
 *
 * Do not feed the finalized value back into update.
 */
uint16_t crc16_ibm_sdlc_finalize(uint16_t state);

/* One-shot convenience: init + a single update + finalize.
 *
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the finished CRC value.
 */
uint16_t crc16_ibm_sdlc(const uint8_t *data, size_t len);

/* Self-test the implementation against independent reference CRCs.
 *
 * Returns 0 iff the generated CRC reproduces every embedded reference value.
 *
 * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 *
 * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 */
int crc16_ibm_sdlc_self_test(void);

#ifndef CRCGLOT_NO_PROVENANCE
#ifndef CRCGLOT_PROVENANCE_T_DEFINED
#define CRCGLOT_PROVENANCE_T_DEFINED
typedef struct {
    const char *version;
    const char *algorithm;
    const char *target;
    const char *variant;
    const char *comment;
    const char *symbol;
    const char *naming;
} crcglot_provenance_t;
#endif
extern const crcglot_provenance_t crc16_ibm_sdlc_provenance;
#endif

#ifdef __cplusplus
}
#endif

#endif /* CRC16_IBM_SDLC_H */
```

**`crc16_ibm_sdlc.c`**

```c
/* crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16_ibm_sdlc(data, len) over "123456789" == 0x906E
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint16_t s = crc16_ibm_sdlc_init();
 *     s = crc16_ibm_sdlc_update(s, chunk, chunk_len);
 *     uint16_t crc = crc16_ibm_sdlc_finalize(s);
 *
 * One-shot: crc16_ibm_sdlc(data, len)
 * Verify:   crc16_ibm_sdlc_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    c
 *     variant:   table
 *     comment:   plain
 *     symbol:    crc16_ibm_sdlc
 *     naming:    snake
 */
#include "crc16_ibm_sdlc.h"

static const uint16_t crcglot_table_crc16_ibm_sdlc[256] = {
    0x0000U, 0x1189U, 0x2312U, 0x329BU, 0x4624U, 0x57ADU, 0x6536U, 0x74BFU,
    0x8C48U, 0x9DC1U, 0xAF5AU, 0xBED3U, 0xCA6CU, 0xDBE5U, 0xE97EU, 0xF8F7U,
    0x1081U, 0x0108U, 0x3393U, 0x221AU, 0x56A5U, 0x472CU, 0x75B7U, 0x643EU,
    0x9CC9U, 0x8D40U, 0xBFDBU, 0xAE52U, 0xDAEDU, 0xCB64U, 0xF9FFU, 0xE876U,
    0x2102U, 0x308BU, 0x0210U, 0x1399U, 0x6726U, 0x76AFU, 0x4434U, 0x55BDU,
    0xAD4AU, 0xBCC3U, 0x8E58U, 0x9FD1U, 0xEB6EU, 0xFAE7U, 0xC87CU, 0xD9F5U,
    0x3183U, 0x200AU, 0x1291U, 0x0318U, 0x77A7U, 0x662EU, 0x54B5U, 0x453CU,
    0xBDCBU, 0xAC42U, 0x9ED9U, 0x8F50U, 0xFBEFU, 0xEA66U, 0xD8FDU, 0xC974U,
    0x4204U, 0x538DU, 0x6116U, 0x709FU, 0x0420U, 0x15A9U, 0x2732U, 0x36BBU,
    0xCE4CU, 0xDFC5U, 0xED5EU, 0xFCD7U, 0x8868U, 0x99E1U, 0xAB7AU, 0xBAF3U,
    0x5285U, 0x430CU, 0x7197U, 0x601EU, 0x14A1U, 0x0528U, 0x37B3U, 0x263AU,
    0xDECDU, 0xCF44U, 0xFDDFU, 0xEC56U, 0x98E9U, 0x8960U, 0xBBFBU, 0xAA72U,
    0x6306U, 0x728FU, 0x4014U, 0x519DU, 0x2522U, 0x34ABU, 0x0630U, 0x17B9U,
    0xEF4EU, 0xFEC7U, 0xCC5CU, 0xDDD5U, 0xA96AU, 0xB8E3U, 0x8A78U, 0x9BF1U,
    0x7387U, 0x620EU, 0x5095U, 0x411CU, 0x35A3U, 0x242AU, 0x16B1U, 0x0738U,
    0xFFCFU, 0xEE46U, 0xDCDDU, 0xCD54U, 0xB9EBU, 0xA862U, 0x9AF9U, 0x8B70U,
    0x8408U, 0x9581U, 0xA71AU, 0xB693U, 0xC22CU, 0xD3A5U, 0xE13EU, 0xF0B7U,
    0x0840U, 0x19C9U, 0x2B52U, 0x3ADBU, 0x4E64U, 0x5FEDU, 0x6D76U, 0x7CFFU,
    0x9489U, 0x8500U, 0xB79BU, 0xA612U, 0xD2ADU, 0xC324U, 0xF1BFU, 0xE036U,
    0x18C1U, 0x0948U, 0x3BD3U, 0x2A5AU, 0x5EE5U, 0x4F6CU, 0x7DF7U, 0x6C7EU,
    0xA50AU, 0xB483U, 0x8618U, 0x9791U, 0xE32EU, 0xF2A7U, 0xC03CU, 0xD1B5U,
    0x2942U, 0x38CBU, 0x0A50U, 0x1BD9U, 0x6F66U, 0x7EEFU, 0x4C74U, 0x5DFDU,
    0xB58BU, 0xA402U, 0x9699U, 0x8710U, 0xF3AFU, 0xE226U, 0xD0BDU, 0xC134U,
    0x39C3U, 0x284AU, 0x1AD1U, 0x0B58U, 0x7FE7U, 0x6E6EU, 0x5CF5U, 0x4D7CU,
    0xC60CU, 0xD785U, 0xE51EU, 0xF497U, 0x8028U, 0x91A1U, 0xA33AU, 0xB2B3U,
    0x4A44U, 0x5BCDU, 0x6956U, 0x78DFU, 0x0C60U, 0x1DE9U, 0x2F72U, 0x3EFBU,
    0xD68DU, 0xC704U, 0xF59FU, 0xE416U, 0x90A9U, 0x8120U, 0xB3BBU, 0xA232U,
    0x5AC5U, 0x4B4CU, 0x79D7U, 0x685EU, 0x1CE1U, 0x0D68U, 0x3FF3U, 0x2E7AU,
    0xE70EU, 0xF687U, 0xC41CU, 0xD595U, 0xA12AU, 0xB0A3U, 0x8238U, 0x93B1U,
    0x6B46U, 0x7ACFU, 0x4854U, 0x59DDU, 0x2D62U, 0x3CEBU, 0x0E70U, 0x1FF9U,
    0xF78FU, 0xE606U, 0xD49DU, 0xC514U, 0xB1ABU, 0xA022U, 0x92B9U, 0x8330U,
    0x7BC7U, 0x6A4EU, 0x58D5U, 0x495CU, 0x3DE3U, 0x2C6AU, 0x1EF1U, 0x0F78U
};

uint16_t crc16_ibm_sdlc_init(void) {
    return 0xFFFFU;
}

uint16_t crc16_ibm_sdlc_update(uint16_t state, const uint8_t *data, size_t len) {
    uint16_t crc = state;
    for (size_t i = 0; i < len; i++) {
        crc = crcglot_table_crc16_ibm_sdlc[(crc ^ data[i]) & 0xFFU] ^ (crc >> 8);
    }
    return crc;
}

uint16_t crc16_ibm_sdlc_finalize(uint16_t state) {
    return state ^ 0xFFFFU;
}

uint16_t crc16_ibm_sdlc(const uint8_t *data, size_t len) {
    return crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_update(crc16_ibm_sdlc_init(), data, len));
}

int crc16_ibm_sdlc_self_test(void) {
    static const uint8_t kCheckInput[] = "123456789";
    int failed = 0;
    if (crc16_ibm_sdlc(kCheckInput, 0) != 0x0000U) {
        failed = 1;
    }
    if (crc16_ibm_sdlc(kCheckInput, 9) != 0x906EU) {
        failed = 1;
    }
    {
        uint16_t s = crc16_ibm_sdlc_init();
        for (unsigned int i = 0U; i < 256U; i++) {
            uint8_t b = (uint8_t)i;
            s = crc16_ibm_sdlc_update(s, &b, 1);
        }
        if (crc16_ibm_sdlc_finalize(s) != 0x303CU) {
            failed = 1;
        }
    }
    {
        uint16_t s = crc16_ibm_sdlc_init();
        for (unsigned int i = 0U; i < 1024U; i++) {
            uint8_t b = (uint8_t)(((i * 167U) + 13U) & 0xFFU);
            s = crc16_ibm_sdlc_update(s, &b, 1);
        }
        if (crc16_ibm_sdlc_finalize(s) != 0x8643U) {
            failed = 1;
        }
    }
    return (failed != 0) ? 1 : 0;
}

#ifndef CRCGLOT_NO_PROVENANCE
const crcglot_provenance_t crc16_ibm_sdlc_provenance = {
    .version   = "0.30.0",
    .algorithm = "crc16-ibm-sdlc",
    .target    = "c",
    .variant   = "table",
    .comment   = "plain",
    .symbol    = "crc16_ibm_sdlc",
    .naming    = "snake",
};
#endif
```

</details>

<a id="lang-csharp"></a>

<details>
<summary>

### C#

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot csharp crc16-ibm-sdlc
```

```csharp
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: Crc16IbmSdlc.Compute(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     ushort s = Crc16IbmSdlc.Init();
//     s = Crc16IbmSdlc.Update(s, chunk);  // over each chunk
//     ushort crc = Crc16IbmSdlc.Finalize(s);
//
// One-shot: Crc16IbmSdlc.Compute(data)
// Verify:   Crc16IbmSdlc.SelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    csharp
//     variant:   table
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    pascal

using System;

public static class Crc16IbmSdlc
{
    private static readonly ushort[] _crcTable = new ushort[] {
        0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
        0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
        0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
        0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
        0x2102, 0x308B, 0x0210, 0x1399, 0x6726, 0x76AF, 0x4434, 0x55BD,
        0xAD4A, 0xBCC3, 0x8E58, 0x9FD1, 0xEB6E, 0xFAE7, 0xC87C, 0xD9F5,
        0x3183, 0x200A, 0x1291, 0x0318, 0x77A7, 0x662E, 0x54B5, 0x453C,
        0xBDCB, 0xAC42, 0x9ED9, 0x8F50, 0xFBEF, 0xEA66, 0xD8FD, 0xC974,
        0x4204, 0x538D, 0x6116, 0x709F, 0x0420, 0x15A9, 0x2732, 0x36BB,
        0xCE4C, 0xDFC5, 0xED5E, 0xFCD7, 0x8868, 0x99E1, 0xAB7A, 0xBAF3,
        0x5285, 0x430C, 0x7197, 0x601E, 0x14A1, 0x0528, 0x37B3, 0x263A,
        0xDECD, 0xCF44, 0xFDDF, 0xEC56, 0x98E9, 0x8960, 0xBBFB, 0xAA72,
        0x6306, 0x728F, 0x4014, 0x519D, 0x2522, 0x34AB, 0x0630, 0x17B9,
        0xEF4E, 0xFEC7, 0xCC5C, 0xDDD5, 0xA96A, 0xB8E3, 0x8A78, 0x9BF1,
        0x7387, 0x620E, 0x5095, 0x411C, 0x35A3, 0x242A, 0x16B1, 0x0738,
        0xFFCF, 0xEE46, 0xDCDD, 0xCD54, 0xB9EB, 0xA862, 0x9AF9, 0x8B70,
        0x8408, 0x9581, 0xA71A, 0xB693, 0xC22C, 0xD3A5, 0xE13E, 0xF0B7,
        0x0840, 0x19C9, 0x2B52, 0x3ADB, 0x4E64, 0x5FED, 0x6D76, 0x7CFF,
        0x9489, 0x8500, 0xB79B, 0xA612, 0xD2AD, 0xC324, 0xF1BF, 0xE036,
        0x18C1, 0x0948, 0x3BD3, 0x2A5A, 0x5EE5, 0x4F6C, 0x7DF7, 0x6C7E,
        0xA50A, 0xB483, 0x8618, 0x9791, 0xE32E, 0xF2A7, 0xC03C, 0xD1B5,
        0x2942, 0x38CB, 0x0A50, 0x1BD9, 0x6F66, 0x7EEF, 0x4C74, 0x5DFD,
        0xB58B, 0xA402, 0x9699, 0x8710, 0xF3AF, 0xE226, 0xD0BD, 0xC134,
        0x39C3, 0x284A, 0x1AD1, 0x0B58, 0x7FE7, 0x6E6E, 0x5CF5, 0x4D7C,
        0xC60C, 0xD785, 0xE51E, 0xF497, 0x8028, 0x91A1, 0xA33A, 0xB2B3,
        0x4A44, 0x5BCD, 0x6956, 0x78DF, 0x0C60, 0x1DE9, 0x2F72, 0x3EFB,
        0xD68D, 0xC704, 0xF59F, 0xE416, 0x90A9, 0x8120, 0xB3BB, 0xA232,
        0x5AC5, 0x4B4C, 0x79D7, 0x685E, 0x1CE1, 0x0D68, 0x3FF3, 0x2E7A,
        0xE70E, 0xF687, 0xC41C, 0xD595, 0xA12A, 0xB0A3, 0x8238, 0x93B1,
        0x6B46, 0x7ACF, 0x4854, 0x59DD, 0x2D62, 0x3CEB, 0x0E70, 0x1FF9,
        0xF78F, 0xE606, 0xD49D, 0xC514, 0xB1AB, 0xA022, 0x92B9, 0x8330,
        0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
    };

    // Return the initial CRC state to begin a computation.
    //
    // Returns the starting ushort state.
    public static ushort Init() {
        return 0xFFFF;
    }

    // Fold input into the running CRC state and return the new state.
    //
    // state: running ushort state (from init or a prior update).
    // data: the message bytes.
    //
    // Returns the updated ushort state (not yet finalized).
    //
    // Call init -> update (any number of times) -> finalize.
    public static ushort Update(ushort state, byte[] data) {
        ushort crc = state;
        foreach (byte b in data) {
            crc = (ushort)(_crcTable[(byte)(crc ^ b)] ^ (crc >> 8));
        }
        return crc;
    }

    // Apply the final XOR to produce the CRC.
    //
    // state: accumulated ushort state from update.
    //
    // Returns the finished CRC value.
    //
    // Do not feed the finalized value back into update.
    public static ushort Finalize(ushort state) {
        return (ushort)(state ^ 0xFFFF);
    }

    // One-shot convenience: init + a single update + finalize.
    //
    // data: the message bytes.
    //
    // Returns the finished CRC value.
    public static ushort Compute(byte[] data) {
        return Finalize(Update(Init(), data));
    }

    // Self-test the implementation against independent reference CRCs.
    //
    // Returns true iff the generated CRC reproduces every embedded reference value.
    //
    // Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
    //
    // Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    public static bool SelfTest() {
        if (Compute(new byte[0]) != 0x0000) return false;
        if (Compute(new byte[] { 0x31, 0x32, 0x33, 0x34, 0x35, 0x36, 0x37, 0x38, 0x39 }) != 0x906E) return false;
        {
            ushort s = Init();
            for (int i = 0; i < 256; i++) {
                s = Update(s, new byte[] { (byte)i });
            }
            if (Finalize(s) != 0x303C) return false;
        }
        {
            ushort s = Init();
            for (int i = 0; i < 1024; i++) {
                s = Update(s, new byte[] { (byte)((i * 167 + 13) & 0xFF) });
            }
            if (Finalize(s) != 0x8643) return false;
        }
        return true;
    }
}
```

</details>

<a id="lang-go"></a>

<details>
<summary>

### Go

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot go crc16-ibm-sdlc
```

```go
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: Crc16IbmSdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     s := Crc16IbmSdlcInit()
//     s = Crc16IbmSdlcUpdate(s, chunk)  // over each chunk of the message
//     crc := Crc16IbmSdlcFinalize(s)
//
// One-shot: Crc16IbmSdlc(data)
// Verify:   Crc16IbmSdlcSelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    go
//     variant:   table
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    pascal

package crc

var _crc16_ibm_sdlc_table = [256]uint16{
    0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
    0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
    0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
    0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
    0x2102, 0x308B, 0x0210, 0x1399, 0x6726, 0x76AF, 0x4434, 0x55BD,
    0xAD4A, 0xBCC3, 0x8E58, 0x9FD1, 0xEB6E, 0xFAE7, 0xC87C, 0xD9F5,
    0x3183, 0x200A, 0x1291, 0x0318, 0x77A7, 0x662E, 0x54B5, 0x453C,
    0xBDCB, 0xAC42, 0x9ED9, 0x8F50, 0xFBEF, 0xEA66, 0xD8FD, 0xC974,
    0x4204, 0x538D, 0x6116, 0x709F, 0x0420, 0x15A9, 0x2732, 0x36BB,
    0xCE4C, 0xDFC5, 0xED5E, 0xFCD7, 0x8868, 0x99E1, 0xAB7A, 0xBAF3,
    0x5285, 0x430C, 0x7197, 0x601E, 0x14A1, 0x0528, 0x37B3, 0x263A,
    0xDECD, 0xCF44, 0xFDDF, 0xEC56, 0x98E9, 0x8960, 0xBBFB, 0xAA72,
    0x6306, 0x728F, 0x4014, 0x519D, 0x2522, 0x34AB, 0x0630, 0x17B9,
    0xEF4E, 0xFEC7, 0xCC5C, 0xDDD5, 0xA96A, 0xB8E3, 0x8A78, 0x9BF1,
    0x7387, 0x620E, 0x5095, 0x411C, 0x35A3, 0x242A, 0x16B1, 0x0738,
    0xFFCF, 0xEE46, 0xDCDD, 0xCD54, 0xB9EB, 0xA862, 0x9AF9, 0x8B70,
    0x8408, 0x9581, 0xA71A, 0xB693, 0xC22C, 0xD3A5, 0xE13E, 0xF0B7,
    0x0840, 0x19C9, 0x2B52, 0x3ADB, 0x4E64, 0x5FED, 0x6D76, 0x7CFF,
    0x9489, 0x8500, 0xB79B, 0xA612, 0xD2AD, 0xC324, 0xF1BF, 0xE036,
    0x18C1, 0x0948, 0x3BD3, 0x2A5A, 0x5EE5, 0x4F6C, 0x7DF7, 0x6C7E,
    0xA50A, 0xB483, 0x8618, 0x9791, 0xE32E, 0xF2A7, 0xC03C, 0xD1B5,
    0x2942, 0x38CB, 0x0A50, 0x1BD9, 0x6F66, 0x7EEF, 0x4C74, 0x5DFD,
    0xB58B, 0xA402, 0x9699, 0x8710, 0xF3AF, 0xE226, 0xD0BD, 0xC134,
    0x39C3, 0x284A, 0x1AD1, 0x0B58, 0x7FE7, 0x6E6E, 0x5CF5, 0x4D7C,
    0xC60C, 0xD785, 0xE51E, 0xF497, 0x8028, 0x91A1, 0xA33A, 0xB2B3,
    0x4A44, 0x5BCD, 0x6956, 0x78DF, 0x0C60, 0x1DE9, 0x2F72, 0x3EFB,
    0xD68D, 0xC704, 0xF59F, 0xE416, 0x90A9, 0x8120, 0xB3BB, 0xA232,
    0x5AC5, 0x4B4C, 0x79D7, 0x685E, 0x1CE1, 0x0D68, 0x3FF3, 0x2E7A,
    0xE70E, 0xF687, 0xC41C, 0xD595, 0xA12A, 0xB0A3, 0x8238, 0x93B1,
    0x6B46, 0x7ACF, 0x4854, 0x59DD, 0x2D62, 0x3CEB, 0x0E70, 0x1FF9,
    0xF78F, 0xE606, 0xD49D, 0xC514, 0xB1AB, 0xA022, 0x92B9, 0x8330,
    0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
}

// Return the initial CRC state to begin a computation.
//
// Returns the starting uint16 state.
func Crc16IbmSdlcInit() uint16 {
    return 0xFFFF
}

// Fold input into the running CRC state and return the new state.
//
// state: running uint16 state (from init or a prior update).
// data: the message bytes.
//
// Returns the updated uint16 state (not yet finalized).
//
// Call init -> update (any number of times) -> finalize.
func Crc16IbmSdlcUpdate(state uint16, data []byte) uint16 {
    crc := state
    for _, b := range data {
        crc = _crc16_ibm_sdlc_table[uint16(byte(crc)^b)] ^ (crc >> 8)
    }
    return crc
}

// Apply the final XOR to produce the CRC.
//
// state: accumulated uint16 state from update.
//
// Returns the finished CRC value.
//
// Do not feed the finalized value back into update.
func Crc16IbmSdlcFinalize(state uint16) uint16 {
    return state ^ 0xFFFF
}

// One-shot convenience: init + a single update + finalize.
//
// data: the message bytes.
//
// Returns the finished CRC value.
func Crc16IbmSdlc(data []byte) uint16 {
    return Crc16IbmSdlcFinalize(Crc16IbmSdlcUpdate(Crc16IbmSdlcInit(), data))
}

// Self-test the implementation against independent reference CRCs.
//
// Returns true iff the generated CRC reproduces every embedded reference value.
//
// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
//
// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
func Crc16IbmSdlcSelfTest() bool {
    if Crc16IbmSdlc([]byte("")) != 0x0000 {
        return false
    }
    if Crc16IbmSdlc([]byte("123456789")) != 0x906E {
        return false
    }
    s := Crc16IbmSdlcInit()
    for i := 0; i < 256; i++ {
        s = Crc16IbmSdlcUpdate(s, []byte{byte(i)})
    }
    if Crc16IbmSdlcFinalize(s) != 0x303C {
        return false
    }
    s = Crc16IbmSdlcInit()
    for i := 0; i < 1024; i++ {
        s = Crc16IbmSdlcUpdate(s, []byte{byte((i*167 + 13) & 0xFF)})
    }
    if Crc16IbmSdlcFinalize(s) != 0x8643 {
        return false
    }
    return true
}
```

</details>

<a id="lang-java"></a>

<details>
<summary>

### Java

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot java crc16-ibm-sdlc
```

```java
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: Crc16IbmSdlc.crc16IbmSdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     int s = Crc16IbmSdlc.crc16IbmSdlcInit();
//     s = Crc16IbmSdlc.crc16IbmSdlcUpdate(s, chunk);  // over each chunk
//     int crc = Crc16IbmSdlc.crc16IbmSdlcFinalize(s);
//
// One-shot: Crc16IbmSdlc.crc16IbmSdlc(data)
// Verify:   Crc16IbmSdlc.crc16IbmSdlcSelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    java
//     variant:   table
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    camel

public final class Crc16IbmSdlc {
    private static final int[] crcglot_table_crc16_ibm_sdlc = {
        0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
        0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
        0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
        0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
        0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
    };

    // Return the initial CRC state to begin a computation.
    //
    // Returns the starting int state.
    public static int crc16IbmSdlcInit() {
        return 0xFFFF;
    }

    // Fold input into the running CRC state and return the new state.
    //
    // state: running int state (from init or a prior update).
    // data: the message bytes.
    //
    // Returns the updated int state (not yet finalized).
    //
    // Call init -> update (any number of times) -> finalize.
    public static int crc16IbmSdlcUpdate(int state, byte[] data) {
        int crc = state;
        for (byte b : data) {
            crc = crcglot_table_crc16_ibm_sdlc[((crc ^ (b & 0xFF)) & 0xFF)] ^ (crc >>> 8);
        }
        return crc;
    }

    // Apply the final XOR to produce the CRC.
    //
    // state: accumulated int state from update.
    //
    // Returns the finished CRC value.
    //
    // Do not feed the finalized value back into update.
    public static int crc16IbmSdlcFinalize(int state) {
        return state ^ 0xFFFF;
    }

    // One-shot convenience: init + a single update + finalize.
    //
    // data: the message bytes.
    //
    // Returns the finished CRC value.
    public static int crc16IbmSdlc(byte[] data) {
        return crc16IbmSdlcFinalize(crc16IbmSdlcUpdate(crc16IbmSdlcInit(), data));
    }

    // Self-test the implementation against independent reference CRCs.
    //
    // Returns true iff the generated CRC reproduces every embedded reference value.
    //
    // Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
    //
    // Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    public static boolean crc16IbmSdlcSelfTest() {
        if (crc16IbmSdlc(new byte[0]) != 0x0000) return false;
        if (crc16IbmSdlc(new byte[] { 0x31, 0x32, 0x33, 0x34, 0x35, 0x36, 0x37, 0x38, 0x39 }) != 0x906E) return false;
        {
            int s = crc16IbmSdlcInit();
            for (int i = 0; i < 256; i++) {
                s = crc16IbmSdlcUpdate(s, new byte[] { (byte) i });
            }
            if (crc16IbmSdlcFinalize(s) != 0x303C) return false;
        }
        {
            int s = crc16IbmSdlcInit();
            for (int i = 0; i < 1024; i++) {
                s = crc16IbmSdlcUpdate(s, new byte[] { (byte) ((i * 167 + 13) & 0xFF) });
            }
            if (crc16IbmSdlcFinalize(s) != 0x8643) return false;
        }
        return true;
    }
}

```

</details>

<a id="lang-lua"></a>

<details>
<summary>

### Lua

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot lua crc16-ibm-sdlc
```

```lua
local crcglot_table_crc16_ibm_sdlc = {
    [0] = 0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
    0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
    0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
    0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
}

-- crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
-- Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
-- check: crc.crc16_ibm_sdlc(data) over "123456789" == 0x906E
--
-- Streaming (init -> update -> finalize); update may be called any
-- number of times over successive chunks of the message:
--
--     local s = crc.crc16_ibm_sdlc_init()
--     s = crc.crc16_ibm_sdlc_update(s, chunk)  -- over each chunk
--     local value = crc.crc16_ibm_sdlc_finalize(s)
--
-- One-shot: crc.crc16_ibm_sdlc(data)
-- Verify:   crc.crc16_ibm_sdlc_self_test() returns true on success; run it once on your
--           toolchain to catch a compiler / endianness mismatch.
--
-- Requires Lua 5.3+ (native 64-bit integers and bitwise operators); not Lua 5.1 / 5.2 / LuaJIT.
--
-- Load with: local crc = dofile("<file>.lua")  (or require, minus the extension).
--
-- Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
-- Reproduce with crcglot:
--     version:   0.30.0
--     algorithm: crc16-ibm-sdlc
--     target:    lua
--     variant:   table
--     comment:   plain
--     symbol:    crc16_ibm_sdlc
--     naming:    snake

local M = {}

-- Return the initial CRC state to begin a computation.
--
-- Returns the starting integer state.
function M.crc16_ibm_sdlc_init()
    return 0xFFFF
end

-- Fold input into the running CRC state and return the new state.
--
-- state: running integer state (from init or a prior update).
-- data: the message bytes (a Lua string).
--
-- Returns the updated integer state (not yet finalized).
--
-- Call init -> update (any number of times) -> finalize.
function M.crc16_ibm_sdlc_update(state, data)
    local crc = state
    for i = 1, #data do
        crc = crcglot_table_crc16_ibm_sdlc[(crc ~ data:byte(i)) & 0xFF] ~ (crc >> 8)
    end
    return crc
end

-- Apply the final XOR to produce the CRC.
--
-- state: accumulated integer state from update.
--
-- Returns the finished CRC value.
--
-- Do not feed the finalized value back into update.
function M.crc16_ibm_sdlc_finalize(state)
    return state ~ 0xFFFF
end

-- One-shot convenience: init + a single update + finalize.
--
-- data: the message bytes (a Lua string).
--
-- Returns the finished CRC value.
function M.crc16_ibm_sdlc(data)
    return M.crc16_ibm_sdlc_finalize(M.crc16_ibm_sdlc_update(M.crc16_ibm_sdlc_init(), data))
end

-- Self-test the implementation against independent reference CRCs.
--
-- Returns true iff the generated CRC reproduces every embedded reference value.
--
-- Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
--
-- Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
function M.crc16_ibm_sdlc_self_test()
    if M.crc16_ibm_sdlc("") ~= 0x0000 then return false end
    if M.crc16_ibm_sdlc("123456789") ~= 0x906E then return false end
    local s = M.crc16_ibm_sdlc_init()
    for i = 0, 255 do
        s = M.crc16_ibm_sdlc_update(s, string.char(i))
    end
    if M.crc16_ibm_sdlc_finalize(s) ~= 0x303C then return false end
    s = M.crc16_ibm_sdlc_init()
    for i = 0, 1023 do
        s = M.crc16_ibm_sdlc_update(s, string.char((i * 167 + 13) & 0xFF))
    end
    if M.crc16_ibm_sdlc_finalize(s) ~= 0x8643 then return false end
    return true
end

return M
```

</details>

<a id="lang-python"></a>

<details>
<summary>

### Python

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot python crc16-ibm-sdlc
```

```python
"""crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
check: crc16_ibm_sdlc(data) over "123456789" == 0x906E

Streaming (init -> update -> finalize); update may be called any
number of times over successive chunks of the message:

    s = crc16_ibm_sdlc_init()
    s = crc16_ibm_sdlc_update(s, chunk)  # over each chunk of the message
    crc = crc16_ibm_sdlc_finalize(s)

One-shot: crc16_ibm_sdlc(data)
Verify:   crc16_ibm_sdlc_self_test() returns True on success; run it once on your
          toolchain to catch a compiler / endianness mismatch.

Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
Reproduce with crcglot:
    version:   0.30.0
    algorithm: crc16-ibm-sdlc
    target:    python
    variant:   table
    comment:   plain
    symbol:    crc16_ibm_sdlc
    naming:    snake
"""

# fmt: off
_crcglot_table_crc16_ibm_sdlc = (
    0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
    0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
    0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
    0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
    0x2102, 0x308B, 0x0210, 0x1399, 0x6726, 0x76AF, 0x4434, 0x55BD,
    0xAD4A, 0xBCC3, 0x8E58, 0x9FD1, 0xEB6E, 0xFAE7, 0xC87C, 0xD9F5,
    0x3183, 0x200A, 0x1291, 0x0318, 0x77A7, 0x662E, 0x54B5, 0x453C,
    0xBDCB, 0xAC42, 0x9ED9, 0x8F50, 0xFBEF, 0xEA66, 0xD8FD, 0xC974,
    0x4204, 0x538D, 0x6116, 0x709F, 0x0420, 0x15A9, 0x2732, 0x36BB,
    0xCE4C, 0xDFC5, 0xED5E, 0xFCD7, 0x8868, 0x99E1, 0xAB7A, 0xBAF3,
    0x5285, 0x430C, 0x7197, 0x601E, 0x14A1, 0x0528, 0x37B3, 0x263A,
    0xDECD, 0xCF44, 0xFDDF, 0xEC56, 0x98E9, 0x8960, 0xBBFB, 0xAA72,
    0x6306, 0x728F, 0x4014, 0x519D, 0x2522, 0x34AB, 0x0630, 0x17B9,
    0xEF4E, 0xFEC7, 0xCC5C, 0xDDD5, 0xA96A, 0xB8E3, 0x8A78, 0x9BF1,
    0x7387, 0x620E, 0x5095, 0x411C, 0x35A3, 0x242A, 0x16B1, 0x0738,
    0xFFCF, 0xEE46, 0xDCDD, 0xCD54, 0xB9EB, 0xA862, 0x9AF9, 0x8B70,
    0x8408, 0x9581, 0xA71A, 0xB693, 0xC22C, 0xD3A5, 0xE13E, 0xF0B7,
    0x0840, 0x19C9, 0x2B52, 0x3ADB, 0x4E64, 0x5FED, 0x6D76, 0x7CFF,
    0x9489, 0x8500, 0xB79B, 0xA612, 0xD2AD, 0xC324, 0xF1BF, 0xE036,
    0x18C1, 0x0948, 0x3BD3, 0x2A5A, 0x5EE5, 0x4F6C, 0x7DF7, 0x6C7E,
    0xA50A, 0xB483, 0x8618, 0x9791, 0xE32E, 0xF2A7, 0xC03C, 0xD1B5,
    0x2942, 0x38CB, 0x0A50, 0x1BD9, 0x6F66, 0x7EEF, 0x4C74, 0x5DFD,
    0xB58B, 0xA402, 0x9699, 0x8710, 0xF3AF, 0xE226, 0xD0BD, 0xC134,
    0x39C3, 0x284A, 0x1AD1, 0x0B58, 0x7FE7, 0x6E6E, 0x5CF5, 0x4D7C,
    0xC60C, 0xD785, 0xE51E, 0xF497, 0x8028, 0x91A1, 0xA33A, 0xB2B3,
    0x4A44, 0x5BCD, 0x6956, 0x78DF, 0x0C60, 0x1DE9, 0x2F72, 0x3EFB,
    0xD68D, 0xC704, 0xF59F, 0xE416, 0x90A9, 0x8120, 0xB3BB, 0xA232,
    0x5AC5, 0x4B4C, 0x79D7, 0x685E, 0x1CE1, 0x0D68, 0x3FF3, 0x2E7A,
    0xE70E, 0xF687, 0xC41C, 0xD595, 0xA12A, 0xB0A3, 0x8238, 0x93B1,
    0x6B46, 0x7ACF, 0x4854, 0x59DD, 0x2D62, 0x3CEB, 0x0E70, 0x1FF9,
    0xF78F, 0xE606, 0xD49D, 0xC514, 0xB1AB, 0xA022, 0x92B9, 0x8330,
    0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
)
# fmt: on


def crc16_ibm_sdlc_init() -> int:
    """Return the initial CRC state to begin a computation.

    Returns the starting int state.
    """
    return 0xFFFF


def crc16_ibm_sdlc_update(state: int, data: bytes) -> int:
    """Fold input into the running CRC state and return the new state.

    state: running int state (from init or a prior update).
    data: the message bytes (a bytes-like object).

    Returns the updated int state (not yet finalized).

    Call init -> update (any number of times) -> finalize.
    """
    crc = state
    for byte in data:
        crc = _crcglot_table_crc16_ibm_sdlc[(crc ^ byte) & 0xFF] ^ (crc >> 8)
    return crc


def crc16_ibm_sdlc_finalize(state: int) -> int:
    """Apply the final XOR to produce the CRC.

    state: accumulated int state from update.

    Returns the finished CRC value.

    Do not feed the finalized value back into update.
    """
    return state ^ 0xFFFF


def crc16_ibm_sdlc(data: bytes) -> int:
    """One-shot convenience: init + a single update + finalize.

    data: the message bytes (a bytes-like object).

    Returns the finished CRC value.
    """
    state = crc16_ibm_sdlc_init()
    state = crc16_ibm_sdlc_update(state, data)
    return crc16_ibm_sdlc_finalize(state)


def crc16_ibm_sdlc_self_test() -> bool:
    """Self-test the implementation against independent reference CRCs.

    Returns True iff the generated CRC reproduces every embedded reference value.

    Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.

    Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    """
    if crc16_ibm_sdlc(b"") != 0x0000:
        return False
    if crc16_ibm_sdlc(b"123456789") != 0x906E:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(256):
        s = crc16_ibm_sdlc_update(s, bytes([i]))
    if crc16_ibm_sdlc_finalize(s) != 0x303C:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(1024):
        s = crc16_ibm_sdlc_update(s, bytes([(i * 167 + 13) & 0xFF]))
    if crc16_ibm_sdlc_finalize(s) != 0x8643:
        return False
    return True
```

</details>

<a id="lang-rust"></a>

<details>
<summary>

### Rust

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot rust crc16-ibm-sdlc
```

```rust
const CRCGLOT_TABLE_CRC16_IBM_SDLC: [u16; 256] = [
    0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
    0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
    0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
    0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
    0x2102, 0x308B, 0x0210, 0x1399, 0x6726, 0x76AF, 0x4434, 0x55BD,
    0xAD4A, 0xBCC3, 0x8E58, 0x9FD1, 0xEB6E, 0xFAE7, 0xC87C, 0xD9F5,
    0x3183, 0x200A, 0x1291, 0x0318, 0x77A7, 0x662E, 0x54B5, 0x453C,
    0xBDCB, 0xAC42, 0x9ED9, 0x8F50, 0xFBEF, 0xEA66, 0xD8FD, 0xC974,
    0x4204, 0x538D, 0x6116, 0x709F, 0x0420, 0x15A9, 0x2732, 0x36BB,
    0xCE4C, 0xDFC5, 0xED5E, 0xFCD7, 0x8868, 0x99E1, 0xAB7A, 0xBAF3,
    0x5285, 0x430C, 0x7197, 0x601E, 0x14A1, 0x0528, 0x37B3, 0x263A,
    0xDECD, 0xCF44, 0xFDDF, 0xEC56, 0x98E9, 0x8960, 0xBBFB, 0xAA72,
    0x6306, 0x728F, 0x4014, 0x519D, 0x2522, 0x34AB, 0x0630, 0x17B9,
    0xEF4E, 0xFEC7, 0xCC5C, 0xDDD5, 0xA96A, 0xB8E3, 0x8A78, 0x9BF1,
    0x7387, 0x620E, 0x5095, 0x411C, 0x35A3, 0x242A, 0x16B1, 0x0738,
    0xFFCF, 0xEE46, 0xDCDD, 0xCD54, 0xB9EB, 0xA862, 0x9AF9, 0x8B70,
    0x8408, 0x9581, 0xA71A, 0xB693, 0xC22C, 0xD3A5, 0xE13E, 0xF0B7,
    0x0840, 0x19C9, 0x2B52, 0x3ADB, 0x4E64, 0x5FED, 0x6D76, 0x7CFF,
    0x9489, 0x8500, 0xB79B, 0xA612, 0xD2AD, 0xC324, 0xF1BF, 0xE036,
    0x18C1, 0x0948, 0x3BD3, 0x2A5A, 0x5EE5, 0x4F6C, 0x7DF7, 0x6C7E,
    0xA50A, 0xB483, 0x8618, 0x9791, 0xE32E, 0xF2A7, 0xC03C, 0xD1B5,
    0x2942, 0x38CB, 0x0A50, 0x1BD9, 0x6F66, 0x7EEF, 0x4C74, 0x5DFD,
    0xB58B, 0xA402, 0x9699, 0x8710, 0xF3AF, 0xE226, 0xD0BD, 0xC134,
    0x39C3, 0x284A, 0x1AD1, 0x0B58, 0x7FE7, 0x6E6E, 0x5CF5, 0x4D7C,
    0xC60C, 0xD785, 0xE51E, 0xF497, 0x8028, 0x91A1, 0xA33A, 0xB2B3,
    0x4A44, 0x5BCD, 0x6956, 0x78DF, 0x0C60, 0x1DE9, 0x2F72, 0x3EFB,
    0xD68D, 0xC704, 0xF59F, 0xE416, 0x90A9, 0x8120, 0xB3BB, 0xA232,
    0x5AC5, 0x4B4C, 0x79D7, 0x685E, 0x1CE1, 0x0D68, 0x3FF3, 0x2E7A,
    0xE70E, 0xF687, 0xC41C, 0xD595, 0xA12A, 0xB0A3, 0x8238, 0x93B1,
    0x6B46, 0x7ACF, 0x4854, 0x59DD, 0x2D62, 0x3CEB, 0x0E70, 0x1FF9,
    0xF78F, 0xE606, 0xD49D, 0xC514, 0xB1AB, 0xA022, 0x92B9, 0x8330,
    0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
];

// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: crc16_ibm_sdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     let s = crc16_ibm_sdlc_init();
//     let s = crc16_ibm_sdlc_update(s, chunk);  // over each chunk
//     let crc = crc16_ibm_sdlc_finalize(s);
//
// One-shot: crc16_ibm_sdlc(data)
// Verify:   crc16_ibm_sdlc_self_test() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    rust
//     variant:   table
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    snake

/// Return the initial CRC state to begin a computation.
///
/// Returns the starting u16 state.
pub fn crc16_ibm_sdlc_init() -> u16 {
    0xFFFF
}

/// Fold input into the running CRC state and return the new state.
///
/// state: running u16 state (from init or a prior update).
/// data: the message bytes.
///
/// Returns the updated u16 state (not yet finalized).
///
/// Call init -> update (any number of times) -> finalize.
pub fn crc16_ibm_sdlc_update(state: u16, data: &[u8]) -> u16 {
    let mut crc: u16 = state;
    for &byte in data {
        crc = CRCGLOT_TABLE_CRC16_IBM_SDLC[(crc ^ byte as u16) as usize & 0xFF] ^ (crc >> 8);
    }
    crc
}

/// Apply the final XOR to produce the CRC.
///
/// state: accumulated u16 state from update.
///
/// Returns the finished CRC value.
///
/// Do not feed the finalized value back into update.
pub fn crc16_ibm_sdlc_finalize(state: u16) -> u16 {
    state ^ 0xFFFF
}

/// One-shot convenience: init + a single update + finalize.
///
/// data: the message bytes.
///
/// Returns the finished CRC value.
pub fn crc16_ibm_sdlc(data: &[u8]) -> u16 {
    crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_update(crc16_ibm_sdlc_init(), data))
}

/// Self-test the implementation against independent reference CRCs.
///
/// Returns true iff the generated CRC reproduces every embedded reference value.
///
/// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
///
/// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
pub fn crc16_ibm_sdlc_self_test() -> bool {
    if crc16_ibm_sdlc(b"") != 0x0000_u16 {
        return false;
    }
    if crc16_ibm_sdlc(b"123456789") != 0x906E_u16 {
        return false;
    }
    let mut s = crc16_ibm_sdlc_init();
    for i in 0u32..256 {
        s = crc16_ibm_sdlc_update(s, &[i as u8]);
    }
    if crc16_ibm_sdlc_finalize(s) != 0x303C_u16 {
        return false;
    }
    s = crc16_ibm_sdlc_init();
    for i in 0u32..1024 {
        s = crc16_ibm_sdlc_update(s, &[((i * 167 + 13) & 0xFF) as u8]);
    }
    if crc16_ibm_sdlc_finalize(s) != 0x8643_u16 {
        return false;
    }
    true
}
```

</details>

<a id="lang-typescript"></a>

<details>
<summary>

### TypeScript

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot typescript crc16-ibm-sdlc
```

```typescript
const crcglot_table_crc16_ibm_sdlc: number[] = [
    0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
    0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
    0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
    0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
    0x2102, 0x308B, 0x0210, 0x1399, 0x6726, 0x76AF, 0x4434, 0x55BD,
    0xAD4A, 0xBCC3, 0x8E58, 0x9FD1, 0xEB6E, 0xFAE7, 0xC87C, 0xD9F5,
    0x3183, 0x200A, 0x1291, 0x0318, 0x77A7, 0x662E, 0x54B5, 0x453C,
    0xBDCB, 0xAC42, 0x9ED9, 0x8F50, 0xFBEF, 0xEA66, 0xD8FD, 0xC974,
    0x4204, 0x538D, 0x6116, 0x709F, 0x0420, 0x15A9, 0x2732, 0x36BB,
    0xCE4C, 0xDFC5, 0xED5E, 0xFCD7, 0x8868, 0x99E1, 0xAB7A, 0xBAF3,
    0x5285, 0x430C, 0x7197, 0x601E, 0x14A1, 0x0528, 0x37B3, 0x263A,
    0xDECD, 0xCF44, 0xFDDF, 0xEC56, 0x98E9, 0x8960, 0xBBFB, 0xAA72,
    0x6306, 0x728F, 0x4014, 0x519D, 0x2522, 0x34AB, 0x0630, 0x17B9,
    0xEF4E, 0xFEC7, 0xCC5C, 0xDDD5, 0xA96A, 0xB8E3, 0x8A78, 0x9BF1,
    0x7387, 0x620E, 0x5095, 0x411C, 0x35A3, 0x242A, 0x16B1, 0x0738,
    0xFFCF, 0xEE46, 0xDCDD, 0xCD54, 0xB9EB, 0xA862, 0x9AF9, 0x8B70,
    0x8408, 0x9581, 0xA71A, 0xB693, 0xC22C, 0xD3A5, 0xE13E, 0xF0B7,
    0x0840, 0x19C9, 0x2B52, 0x3ADB, 0x4E64, 0x5FED, 0x6D76, 0x7CFF,
    0x9489, 0x8500, 0xB79B, 0xA612, 0xD2AD, 0xC324, 0xF1BF, 0xE036,
    0x18C1, 0x0948, 0x3BD3, 0x2A5A, 0x5EE5, 0x4F6C, 0x7DF7, 0x6C7E,
    0xA50A, 0xB483, 0x8618, 0x9791, 0xE32E, 0xF2A7, 0xC03C, 0xD1B5,
    0x2942, 0x38CB, 0x0A50, 0x1BD9, 0x6F66, 0x7EEF, 0x4C74, 0x5DFD,
    0xB58B, 0xA402, 0x9699, 0x8710, 0xF3AF, 0xE226, 0xD0BD, 0xC134,
    0x39C3, 0x284A, 0x1AD1, 0x0B58, 0x7FE7, 0x6E6E, 0x5CF5, 0x4D7C,
    0xC60C, 0xD785, 0xE51E, 0xF497, 0x8028, 0x91A1, 0xA33A, 0xB2B3,
    0x4A44, 0x5BCD, 0x6956, 0x78DF, 0x0C60, 0x1DE9, 0x2F72, 0x3EFB,
    0xD68D, 0xC704, 0xF59F, 0xE416, 0x90A9, 0x8120, 0xB3BB, 0xA232,
    0x5AC5, 0x4B4C, 0x79D7, 0x685E, 0x1CE1, 0x0D68, 0x3FF3, 0x2E7A,
    0xE70E, 0xF687, 0xC41C, 0xD595, 0xA12A, 0xB0A3, 0x8238, 0x93B1,
    0x6B46, 0x7ACF, 0x4854, 0x59DD, 0x2D62, 0x3CEB, 0x0E70, 0x1FF9,
    0xF78F, 0xE606, 0xD49D, 0xC514, 0xB1AB, 0xA022, 0x92B9, 0x8330,
    0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
];

// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: crc16IbmSdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     let s = crc16IbmSdlcInit();
//     s = crc16IbmSdlcUpdate(s, chunk);  // over each chunk of the message
//     const crc = crc16IbmSdlcFinalize(s);
//
// One-shot: crc16IbmSdlc(data)
// Verify:   crc16IbmSdlcSelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    typescript
//     variant:   table
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    camel

// Return the initial CRC state to begin a computation.
//
// Returns the starting number state.
export function crc16IbmSdlcInit(): number {
    return 0xFFFF;
}

// Fold input into the running CRC state and return the new state.
//
// state: running number state (from init or a prior update).
// data: the message bytes.
//
// Returns the updated number state (not yet finalized).
//
// Call init -> update (any number of times) -> finalize.
export function crc16IbmSdlcUpdate(state: number, data: Uint8Array): number {
    let crc: number = state;
    for (const byte of data) {
        crc = crcglot_table_crc16_ibm_sdlc[(crc ^ byte) & 0xFF] ^ (crc >>> 8);
    }
    return crc;
}

// Apply the final XOR to produce the CRC.
//
// state: accumulated number state from update.
//
// Returns the finished CRC value.
//
// Do not feed the finalized value back into update.
export function crc16IbmSdlcFinalize(state: number): number {
    return state ^ 0xFFFF;
}

// One-shot convenience: init + a single update + finalize.
//
// data: the message bytes.
//
// Returns the finished CRC value.
export function crc16IbmSdlc(data: Uint8Array): number {
    return crc16IbmSdlcFinalize(crc16IbmSdlcUpdate(crc16IbmSdlcInit(), data));
}

// Self-test the implementation against independent reference CRCs.
//
// Returns true iff the generated CRC reproduces every embedded reference value.
//
// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
//
// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
export function crc16IbmSdlcSelfTest(): boolean {
    if (crc16IbmSdlc(new Uint8Array([])) !== 0x0000) return false;
    if (crc16IbmSdlc(new Uint8Array([0x31,0x32,0x33,0x34,0x35,0x36,0x37,0x38,0x39])) !== 0x906E) return false;
    {
        let s = crc16IbmSdlcInit();
        for (let i = 0; i < 256; i++) {
            s = crc16IbmSdlcUpdate(s, new Uint8Array([i]));
        }
        if (crc16IbmSdlcFinalize(s) !== 0x303C) return false;
    }
    {
        let s = crc16IbmSdlcInit();
        for (let i = 0; i < 1024; i++) {
            s = crc16IbmSdlcUpdate(s, new Uint8Array([(i * 167 + 13) & 0xFF]));
        }
        if (crc16IbmSdlcFinalize(s) !== 0x8643) return false;
    }
    return true;
}
```

</details>

<a id="lang-verilog"></a>

<details>
<summary>

### Verilog

</summary>

_Bit-by-bit: Smallest code, no lookup table.  Works for any width._

```bash
crcglot verilog crc16-ibm-sdlc
```

```systemverilog
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: crc16_ibm_sdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     state = crc16_ibm_sdlc_init();
//     state = crc16_ibm_sdlc_update(state, byte_in);  // call once per byte
//     crc = crc16_ibm_sdlc_finalize(state);
//
// One-shot: crc16_ibm_sdlc(data)
// Verify:   crc16_ibm_sdlc_self_test returns 1'b1 on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// update consumes ONE byte per call -- the one-shot wrapper loops over the array for you.
//
// Simulator reference (verified under ghdl / iverilog), not a drop-in synthesizable RTL core.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    verilog
//     variant:   bitwise
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    snake

`ifndef CRC16_IBM_SDLC_PKG_SV
`define CRC16_IBM_SDLC_PKG_SV

package crc16_ibm_sdlc_pkg;

    // Return the initial CRC state to begin a computation.
    //
    // Returns the starting 16-bit state.
    function automatic [15:0] crc16_ibm_sdlc_init();
        crc16_ibm_sdlc_init = 16'hFFFF;
    endfunction

    // Fold input into the running CRC state and return the new state.
    //
    // state: running 16-bit state (from init or a prior update).
    // byte_in: a single input byte (8 bits).
    //
    // Returns the updated 16-bit state (not yet finalized).
    //
    // Call init -> update (any number of times) -> finalize.
    //
    // Consumes one byte per call -- loop over your message bytes.
    function automatic [15:0] crc16_ibm_sdlc_update(input [15:0] state, input [7:0] byte_in);
        logic [15:0] crc;
        crc = state;
        crc = crc ^ {{8{1'b0}}, byte_in};
        for (int j = 0; j < 8; j++) begin
            if (crc[0] == 1'b1) begin
                crc = (crc >> 1) ^ 16'h8408;
            end else begin
                crc = crc >> 1;
            end
        end
        crc16_ibm_sdlc_update = crc;
    endfunction

    // Apply the final XOR to produce the CRC.
    //
    // state: accumulated 16-bit state from update.
    //
    // Returns the finished CRC value.
    //
    // Do not feed the finalized value back into update.
    function automatic [15:0] crc16_ibm_sdlc_finalize(input [15:0] state);
        logic [15:0] crc;
        crc = state;
        crc16_ibm_sdlc_finalize = crc ^ 16'hFFFF;
    endfunction

    // One-shot convenience: init + a single update + finalize.
    //
    // data: the packed message as a byte-unsigned array.
    //
    // Returns the finished CRC value.
    function automatic [15:0] crc16_ibm_sdlc(input byte unsigned data[]);
        logic [15:0] s;
        s = crc16_ibm_sdlc_init();
        foreach (data[i]) begin
            s = crc16_ibm_sdlc_update(s, data[i]);
        end
        crc16_ibm_sdlc = crc16_ibm_sdlc_finalize(s);
    endfunction

    // Self-test the implementation against independent reference CRCs.
    //
    // Returns 1'b1 iff the generated CRC reproduces every embedded reference value.
    //
    // Checks two fixed inputs -- the empty string and "123456789" -- against references from two independent engines that had to agree.  These targets are bitwise with no lookup table, so the larger table-coverage vectors the software targets use are omitted; the empty input still exercises the init-then-finalize path with no message bytes.
    //
    // Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    function automatic bit crc16_ibm_sdlc_self_test();
        byte unsigned empty_data[];
        byte unsigned check_data[] = '{
            8'h31, 8'h32, 8'h33, 8'h34, 8'h35,
            8'h36, 8'h37, 8'h38, 8'h39
        };
        empty_data = new[0];
        return (crc16_ibm_sdlc(empty_data) == 16'h0000)
            && (crc16_ibm_sdlc(check_data) == 16'h906E);
    endfunction

endpackage

`endif
```

</details>

<a id="lang-vhdl"></a>

<details>
<summary>

### VHDL

</summary>

_Bit-by-bit: Smallest code, no lookup table.  Works for any width._

```bash
crcglot vhdl crc16-ibm-sdlc
```

```vhdl
-- crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
-- Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
-- check: crc16_ibm_sdlc(data) over "123456789" == 0x906E
--
-- Streaming (init -> update -> finalize); update may be called any
-- number of times over successive chunks of the message:
--
--     s := crc16_ibm_sdlc_init();
--     s := crc16_ibm_sdlc_update(s, chunk);  -- chunk length a multiple of 8
--     crc := crc16_ibm_sdlc_finalize(s);
--
-- One-shot: crc16_ibm_sdlc(data)
-- Verify:   crc16_ibm_sdlc_self_test returns true on success; run it once on your
--           toolchain to catch a compiler / endianness mismatch.
--
-- Input vectors are packed bytes; the length must be a multiple of 8 bits.
--
-- Simulator reference (verified under ghdl), not a drop-in synthesizable RTL core.
--
-- Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
-- Reproduce with crcglot:
--     version:   0.30.0
--     algorithm: crc16-ibm-sdlc
--     target:    vhdl
--     variant:   bitwise
--     comment:   plain
--     symbol:    crc16_ibm_sdlc
--     naming:    snake

library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

package crc16_ibm_sdlc_pkg is

    -- Return the initial CRC state to begin a computation.
    --
    -- Returns the starting 16-bit std_logic_vector state.
    function crc16_ibm_sdlc_init return std_logic_vector;

    -- Fold input into the running CRC state and return the new state.
    --
    -- state: running 16-bit std_logic_vector state (from init or a prior update).
    -- data: the message as a packed std_logic_vector (length a multiple of 8).
    --
    -- Returns the updated 16-bit std_logic_vector state (not yet finalized).
    --
    -- Call init -> update (any number of times) -> finalize.
    function crc16_ibm_sdlc_update(state: std_logic_vector; data: std_logic_vector) return std_logic_vector;

    -- Apply the final XOR to produce the CRC.
    --
    -- state: accumulated 16-bit std_logic_vector state from update.
    --
    -- Returns the finished CRC value.
    --
    -- Do not feed the finalized value back into update.
    function crc16_ibm_sdlc_finalize(state: std_logic_vector) return std_logic_vector;

    -- One-shot convenience: init + a single update + finalize.
    --
    -- data: the message as a packed std_logic_vector (length a multiple of 8).
    --
    -- Returns the finished CRC value.
    function crc16_ibm_sdlc(data: std_logic_vector) return std_logic_vector;

    -- Self-test the implementation against independent reference CRCs.
    --
    -- Returns true iff the generated CRC reproduces every embedded reference value.
    --
    -- Checks two fixed inputs -- the empty string and "123456789" -- against references from two independent engines that had to agree.  These targets are bitwise with no lookup table, so the larger table-coverage vectors the software targets use are omitted; the empty input still exercises the init-then-finalize path with no message bytes.
    --
    -- Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    function crc16_ibm_sdlc_self_test return boolean;
end package;

package body crc16_ibm_sdlc_pkg is

    function crc16_ibm_sdlc_init return std_logic_vector is
        variable s: unsigned(15 downto 0) := x"FFFF";
    begin
        return std_logic_vector(s);
    end function;

    function crc16_ibm_sdlc_update(state: std_logic_vector; data: std_logic_vector) return std_logic_vector is
        variable crc: unsigned(15 downto 0) := unsigned(state);
        variable byte: unsigned(7 downto 0);
        -- Normalize indexing regardless of caller's slice direction.
        constant d: std_logic_vector(data'length - 1 downto 0) := data;
        constant n: natural := data'length / 8;
    begin
        for i in 0 to n - 1 loop
            byte := unsigned(d((n - i)*8 - 1 downto (n - i - 1)*8));
            crc := crc xor resize(byte, 16);
            for j in 0 to 7 loop
                if crc(0) = '1' then
                    crc := shift_right(crc, 1) xor x"8408";
                else
                    crc := shift_right(crc, 1);
                end if;
            end loop;
        end loop;
        return std_logic_vector(crc);
    end function;

    function crc16_ibm_sdlc_finalize(state: std_logic_vector) return std_logic_vector is
        variable crc: unsigned(15 downto 0) := unsigned(state);
    begin
        return std_logic_vector(crc xor x"FFFF");
    end function;

    function crc16_ibm_sdlc(data: std_logic_vector) return std_logic_vector is
    begin
        return crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_update(crc16_ibm_sdlc_init, data));
    end function;

    -- Check the empty input and the reveng check string; true on success.
    function crc16_ibm_sdlc_self_test return boolean is
        constant kCheckInput: std_logic_vector(71 downto 0) :=
            x"313233343536373839";  -- ASCII "123456789"
    begin
        return unsigned(crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_init)) = x"0000"
            and unsigned(crc16_ibm_sdlc(kCheckInput)) = x"906E";
    end function;
end package body;
```

</details>

<a id="lang-zig"></a>

<details>
<summary>

### Zig

</summary>

_Table-driven: One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot zig crc16-ibm-sdlc
```

```zig
const crcglot_table_crc16_ibm_sdlc = [256]u16{
    0x0000, 0x1189, 0x2312, 0x329B, 0x4624, 0x57AD, 0x6536, 0x74BF,
    0x8C48, 0x9DC1, 0xAF5A, 0xBED3, 0xCA6C, 0xDBE5, 0xE97E, 0xF8F7,
    0x1081, 0x0108, 0x3393, 0x221A, 0x56A5, 0x472C, 0x75B7, 0x643E,
    0x9CC9, 0x8D40, 0xBFDB, 0xAE52, 0xDAED, 0xCB64, 0xF9FF, 0xE876,
    0x2102, 0x308B, 0x0210, 0x1399, 0x6726, 0x76AF, 0x4434, 0x55BD,
    0xAD4A, 0xBCC3, 0x8E58, 0x9FD1, 0xEB6E, 0xFAE7, 0xC87C, 0xD9F5,
    0x3183, 0x200A, 0x1291, 0x0318, 0x77A7, 0x662E, 0x54B5, 0x453C,
    0xBDCB, 0xAC42, 0x9ED9, 0x8F50, 0xFBEF, 0xEA66, 0xD8FD, 0xC974,
    0x4204, 0x538D, 0x6116, 0x709F, 0x0420, 0x15A9, 0x2732, 0x36BB,
    0xCE4C, 0xDFC5, 0xED5E, 0xFCD7, 0x8868, 0x99E1, 0xAB7A, 0xBAF3,
    0x5285, 0x430C, 0x7197, 0x601E, 0x14A1, 0x0528, 0x37B3, 0x263A,
    0xDECD, 0xCF44, 0xFDDF, 0xEC56, 0x98E9, 0x8960, 0xBBFB, 0xAA72,
    0x6306, 0x728F, 0x4014, 0x519D, 0x2522, 0x34AB, 0x0630, 0x17B9,
    0xEF4E, 0xFEC7, 0xCC5C, 0xDDD5, 0xA96A, 0xB8E3, 0x8A78, 0x9BF1,
    0x7387, 0x620E, 0x5095, 0x411C, 0x35A3, 0x242A, 0x16B1, 0x0738,
    0xFFCF, 0xEE46, 0xDCDD, 0xCD54, 0xB9EB, 0xA862, 0x9AF9, 0x8B70,
    0x8408, 0x9581, 0xA71A, 0xB693, 0xC22C, 0xD3A5, 0xE13E, 0xF0B7,
    0x0840, 0x19C9, 0x2B52, 0x3ADB, 0x4E64, 0x5FED, 0x6D76, 0x7CFF,
    0x9489, 0x8500, 0xB79B, 0xA612, 0xD2AD, 0xC324, 0xF1BF, 0xE036,
    0x18C1, 0x0948, 0x3BD3, 0x2A5A, 0x5EE5, 0x4F6C, 0x7DF7, 0x6C7E,
    0xA50A, 0xB483, 0x8618, 0x9791, 0xE32E, 0xF2A7, 0xC03C, 0xD1B5,
    0x2942, 0x38CB, 0x0A50, 0x1BD9, 0x6F66, 0x7EEF, 0x4C74, 0x5DFD,
    0xB58B, 0xA402, 0x9699, 0x8710, 0xF3AF, 0xE226, 0xD0BD, 0xC134,
    0x39C3, 0x284A, 0x1AD1, 0x0B58, 0x7FE7, 0x6E6E, 0x5CF5, 0x4D7C,
    0xC60C, 0xD785, 0xE51E, 0xF497, 0x8028, 0x91A1, 0xA33A, 0xB2B3,
    0x4A44, 0x5BCD, 0x6956, 0x78DF, 0x0C60, 0x1DE9, 0x2F72, 0x3EFB,
    0xD68D, 0xC704, 0xF59F, 0xE416, 0x90A9, 0x8120, 0xB3BB, 0xA232,
    0x5AC5, 0x4B4C, 0x79D7, 0x685E, 0x1CE1, 0x0D68, 0x3FF3, 0x2E7A,
    0xE70E, 0xF687, 0xC41C, 0xD595, 0xA12A, 0xB0A3, 0x8238, 0x93B1,
    0x6B46, 0x7ACF, 0x4854, 0x59DD, 0x2D62, 0x3CEB, 0x0E70, 0x1FF9,
    0xF78F, 0xE606, 0xD49D, 0xC514, 0xB1AB, 0xA022, 0x92B9, 0x8330,
    0x7BC7, 0x6A4E, 0x58D5, 0x495C, 0x3DE3, 0x2C6A, 0x1EF1, 0x0F78,
};

// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: crc16IbmSdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     var s = crc16IbmSdlcInit();
//     s = crc16IbmSdlcUpdate(s, chunk);  // over each chunk
//     const crc = crc16IbmSdlcFinalize(s);
//
// One-shot: crc16IbmSdlc(data)
// Verify:   crc16IbmSdlcSelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Import-free and pure: also runs at comptime (raise @setEvalBranchQuota for long inputs).
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    zig
//     variant:   table
//     comment:   plain
//     symbol:    crc16_ibm_sdlc
//     naming:    camel

/// Return the initial CRC state to begin a computation.
///
/// Returns the starting u16 state.
pub fn crc16IbmSdlcInit() u16 {
    return 0xFFFF;
}

/// Fold input into the running CRC state and return the new state.
///
/// state: running u16 state (from init or a prior update).
/// data: the message bytes.
///
/// Returns the updated u16 state (not yet finalized).
///
/// Call init -> update (any number of times) -> finalize.
pub fn crc16IbmSdlcUpdate(state: u16, data: []const u8) u16 {
    var crc: u16 = state;
    for (data) |b| {
        crc = crcglot_table_crc16_ibm_sdlc[@as(usize, @as(u8, @truncate(crc)) ^ b)] ^ (crc >> 8);
    }
    return crc;
}

/// Apply the final XOR to produce the CRC.
///
/// state: accumulated u16 state from update.
///
/// Returns the finished CRC value.
///
/// Do not feed the finalized value back into update.
pub fn crc16IbmSdlcFinalize(state: u16) u16 {
    return state ^ 0xFFFF;
}

/// One-shot convenience: init + a single update + finalize.
///
/// data: the message bytes.
///
/// Returns the finished CRC value.
pub fn crc16IbmSdlc(data: []const u8) u16 {
    return crc16IbmSdlcFinalize(crc16IbmSdlcUpdate(crc16IbmSdlcInit(), data));
}

/// Self-test the implementation against independent reference CRCs.
///
/// Returns true iff the generated CRC reproduces every embedded reference value.
///
/// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
///
/// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
pub fn crc16IbmSdlcSelfTest() bool {
    if (crc16IbmSdlc("") != 0x0000) return false;
    if (crc16IbmSdlc("123456789") != 0x906E) return false;
    var s = crc16IbmSdlcInit();
    var i: u32 = 0;
    while (i < 256) : (i += 1) {
        s = crc16IbmSdlcUpdate(s, &[_]u8{@as(u8, @truncate(i))});
    }
    if (crc16IbmSdlcFinalize(s) != 0x303C) return false;
    s = crc16IbmSdlcInit();
    i = 0;
    while (i < 1024) : (i += 1) {
        s = crc16IbmSdlcUpdate(s, &[_]u8{@as(u8, @truncate(i * 167 + 13))});
    }
    if (crc16IbmSdlcFinalize(s) != 0x8643) return false;
    return true;
}

// `zig test <file>.zig` runs this; normal builds strip it.
test "crc16IbmSdlc self-test" {
    if (!crc16IbmSdlcSelfTest()) return error.SelfTestFailed;
}
```

</details>

## Implementation variants

`crc32-bzip2` in C / C++ three times, so the only difference between blocks is the implementation strategy.  Slice-by-8 is why this tour uses a 32-bit algorithm: it exists only at width 32 and 64.  `--fast` is the default, and picks the last variant the target supports at the algorithm's width.

<a id="variant-bitwise"></a>

<details>
<summary>

### Bit-by-bit

</summary>

_Smallest code, no lookup table.  Works for any width._

```bash
crcglot c crc32-bzip2 --small
```

**`crc32_bzip2.h`**

```c
/* crc32-bzip2 -- bzip2 file compression, AAL5
 * Parameters: width=32, poly=0x04C11DB7, init=0xFFFFFFFF, refin=false, refout=false, xorout=0xFFFFFFFF.
 * check: crc32_bzip2(data, len) over "123456789" == 0xFC891918
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint32_t s = crc32_bzip2_init();
 *     s = crc32_bzip2_update(s, chunk, chunk_len);
 *     uint32_t crc = crc32_bzip2_finalize(s);
 *
 * One-shot: crc32_bzip2(data, len)
 * Verify:   crc32_bzip2_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc32-bzip2 -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc32-bzip2
 *     target:    c
 *     variant:   bitwise
 *     comment:   plain
 *     symbol:    crc32_bzip2
 *     naming:    snake
 */
#ifndef CRC32_BZIP2_H
#define CRC32_BZIP2_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Return the initial CRC state to begin a computation.
 *
 * Returns the starting uint32_t state.
 */
uint32_t crc32_bzip2_init(void);

/* Fold input into the running CRC state and return the new state.
 *
 * state: running uint32_t state (from init or a prior update).
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the updated uint32_t state (not yet finalized).
 *
 * Call init -> update (any number of times) -> finalize.
 */
uint32_t crc32_bzip2_update(uint32_t state, const uint8_t *data, size_t len);

/* Apply the final XOR to produce the CRC.
 *
 * state: accumulated uint32_t state from update.
 *
 * Returns the finished CRC value.
 *
 * Do not feed the finalized value back into update.
 */
uint32_t crc32_bzip2_finalize(uint32_t state);

/* One-shot convenience: init + a single update + finalize.
 *
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the finished CRC value.
 */
uint32_t crc32_bzip2(const uint8_t *data, size_t len);

/* Self-test the implementation against independent reference CRCs.
 *
 * Returns 0 iff the generated CRC reproduces every embedded reference value.
 *
 * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 *
 * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 */
int crc32_bzip2_self_test(void);

#ifndef CRCGLOT_NO_PROVENANCE
#ifndef CRCGLOT_PROVENANCE_T_DEFINED
#define CRCGLOT_PROVENANCE_T_DEFINED
typedef struct {
    const char *version;
    const char *algorithm;
    const char *target;
    const char *variant;
    const char *comment;
    const char *symbol;
    const char *naming;
} crcglot_provenance_t;
#endif
extern const crcglot_provenance_t crc32_bzip2_provenance;
#endif

#ifdef __cplusplus
}
#endif

#endif /* CRC32_BZIP2_H */
```

**`crc32_bzip2.c`**

```c
/* crc32-bzip2 -- bzip2 file compression, AAL5
 * Parameters: width=32, poly=0x04C11DB7, init=0xFFFFFFFF, refin=false, refout=false, xorout=0xFFFFFFFF.
 * check: crc32_bzip2(data, len) over "123456789" == 0xFC891918
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint32_t s = crc32_bzip2_init();
 *     s = crc32_bzip2_update(s, chunk, chunk_len);
 *     uint32_t crc = crc32_bzip2_finalize(s);
 *
 * One-shot: crc32_bzip2(data, len)
 * Verify:   crc32_bzip2_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc32-bzip2 -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc32-bzip2
 *     target:    c
 *     variant:   bitwise
 *     comment:   plain
 *     symbol:    crc32_bzip2
 *     naming:    snake
 */
#include "crc32_bzip2.h"

uint32_t crc32_bzip2_init(void) {
    return 0xFFFFFFFFU;
}

uint32_t crc32_bzip2_update(uint32_t state, const uint8_t *data, size_t len) {
    uint32_t crc = state;
    for (size_t i = 0; i < len; i++) {
        crc ^= (uint32_t)data[i] << 24;
        for (int j = 0; j < 8; j++) {
            if ((crc & 0x80000000U) != 0U) {
                crc = (crc << 1) ^ 0x04C11DB7U;
            } else {
                crc <<= 1;
            }
            crc &= 0xFFFFFFFFU;
        }
    }
    return crc;
}

uint32_t crc32_bzip2_finalize(uint32_t state) {
    return state ^ 0xFFFFFFFFU;
}

uint32_t crc32_bzip2(const uint8_t *data, size_t len) {
    return crc32_bzip2_finalize(crc32_bzip2_update(crc32_bzip2_init(), data, len));
}

int crc32_bzip2_self_test(void) {
    static const uint8_t kCheckInput[] = "123456789";
    int failed = 0;
    if (crc32_bzip2(kCheckInput, 0) != 0x00000000U) {
        failed = 1;
    }
    if (crc32_bzip2(kCheckInput, 9) != 0xFC891918U) {
        failed = 1;
    }
    {
        uint32_t s = crc32_bzip2_init();
        for (unsigned int i = 0U; i < 256U; i++) {
            uint8_t b = (uint8_t)i;
            s = crc32_bzip2_update(s, &b, 1);
        }
        if (crc32_bzip2_finalize(s) != 0xB6B5EE95U) {
            failed = 1;
        }
    }
    {
        uint32_t s = crc32_bzip2_init();
        for (unsigned int i = 0U; i < 1024U; i++) {
            uint8_t b = (uint8_t)(((i * 167U) + 13U) & 0xFFU);
            s = crc32_bzip2_update(s, &b, 1);
        }
        if (crc32_bzip2_finalize(s) != 0x646333CAU) {
            failed = 1;
        }
    }
    return (failed != 0) ? 1 : 0;
}

#ifndef CRCGLOT_NO_PROVENANCE
const crcglot_provenance_t crc32_bzip2_provenance = {
    .version   = "0.30.0",
    .algorithm = "crc32-bzip2",
    .target    = "c",
    .variant   = "bitwise",
    .comment   = "plain",
    .symbol    = "crc32_bzip2",
    .naming    = "snake",
};
#endif
```

</details>

<a id="variant-table"></a>

<details>
<summary>

### Table-driven

</summary>

_One 256-entry lookup table; ~10x faster than bit-by-bit._

```bash
crcglot c crc32-bzip2 --table
```

**`crc32_bzip2.h`**

```c
/* crc32-bzip2 -- bzip2 file compression, AAL5
 * Parameters: width=32, poly=0x04C11DB7, init=0xFFFFFFFF, refin=false, refout=false, xorout=0xFFFFFFFF.
 * check: crc32_bzip2(data, len) over "123456789" == 0xFC891918
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint32_t s = crc32_bzip2_init();
 *     s = crc32_bzip2_update(s, chunk, chunk_len);
 *     uint32_t crc = crc32_bzip2_finalize(s);
 *
 * One-shot: crc32_bzip2(data, len)
 * Verify:   crc32_bzip2_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc32-bzip2 -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc32-bzip2
 *     target:    c
 *     variant:   table
 *     comment:   plain
 *     symbol:    crc32_bzip2
 *     naming:    snake
 */
#ifndef CRC32_BZIP2_H
#define CRC32_BZIP2_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Return the initial CRC state to begin a computation.
 *
 * Returns the starting uint32_t state.
 */
uint32_t crc32_bzip2_init(void);

/* Fold input into the running CRC state and return the new state.
 *
 * state: running uint32_t state (from init or a prior update).
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the updated uint32_t state (not yet finalized).
 *
 * Call init -> update (any number of times) -> finalize.
 */
uint32_t crc32_bzip2_update(uint32_t state, const uint8_t *data, size_t len);

/* Apply the final XOR to produce the CRC.
 *
 * state: accumulated uint32_t state from update.
 *
 * Returns the finished CRC value.
 *
 * Do not feed the finalized value back into update.
 */
uint32_t crc32_bzip2_finalize(uint32_t state);

/* One-shot convenience: init + a single update + finalize.
 *
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the finished CRC value.
 */
uint32_t crc32_bzip2(const uint8_t *data, size_t len);

/* Self-test the implementation against independent reference CRCs.
 *
 * Returns 0 iff the generated CRC reproduces every embedded reference value.
 *
 * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 *
 * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 */
int crc32_bzip2_self_test(void);

#ifndef CRCGLOT_NO_PROVENANCE
#ifndef CRCGLOT_PROVENANCE_T_DEFINED
#define CRCGLOT_PROVENANCE_T_DEFINED
typedef struct {
    const char *version;
    const char *algorithm;
    const char *target;
    const char *variant;
    const char *comment;
    const char *symbol;
    const char *naming;
} crcglot_provenance_t;
#endif
extern const crcglot_provenance_t crc32_bzip2_provenance;
#endif

#ifdef __cplusplus
}
#endif

#endif /* CRC32_BZIP2_H */
```

**`crc32_bzip2.c`**

```c
/* crc32-bzip2 -- bzip2 file compression, AAL5
 * Parameters: width=32, poly=0x04C11DB7, init=0xFFFFFFFF, refin=false, refout=false, xorout=0xFFFFFFFF.
 * check: crc32_bzip2(data, len) over "123456789" == 0xFC891918
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint32_t s = crc32_bzip2_init();
 *     s = crc32_bzip2_update(s, chunk, chunk_len);
 *     uint32_t crc = crc32_bzip2_finalize(s);
 *
 * One-shot: crc32_bzip2(data, len)
 * Verify:   crc32_bzip2_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc32-bzip2 -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc32-bzip2
 *     target:    c
 *     variant:   table
 *     comment:   plain
 *     symbol:    crc32_bzip2
 *     naming:    snake
 */
#include "crc32_bzip2.h"

static const uint32_t crcglot_table_crc32_bzip2[256] = {
    0x00000000U, 0x04C11DB7U, 0x09823B6EU, 0x0D4326D9U, 0x130476DCU, 0x17C56B6BU, 0x1A864DB2U, 0x1E475005U,
    0x2608EDB8U, 0x22C9F00FU, 0x2F8AD6D6U, 0x2B4BCB61U, 0x350C9B64U, 0x31CD86D3U, 0x3C8EA00AU, 0x384FBDBDU,
    0x4C11DB70U, 0x48D0C6C7U, 0x4593E01EU, 0x4152FDA9U, 0x5F15ADACU, 0x5BD4B01BU, 0x569796C2U, 0x52568B75U,
    0x6A1936C8U, 0x6ED82B7FU, 0x639B0DA6U, 0x675A1011U, 0x791D4014U, 0x7DDC5DA3U, 0x709F7B7AU, 0x745E66CDU,
    0x9823B6E0U, 0x9CE2AB57U, 0x91A18D8EU, 0x95609039U, 0x8B27C03CU, 0x8FE6DD8BU, 0x82A5FB52U, 0x8664E6E5U,
    0xBE2B5B58U, 0xBAEA46EFU, 0xB7A96036U, 0xB3687D81U, 0xAD2F2D84U, 0xA9EE3033U, 0xA4AD16EAU, 0xA06C0B5DU,
    0xD4326D90U, 0xD0F37027U, 0xDDB056FEU, 0xD9714B49U, 0xC7361B4CU, 0xC3F706FBU, 0xCEB42022U, 0xCA753D95U,
    0xF23A8028U, 0xF6FB9D9FU, 0xFBB8BB46U, 0xFF79A6F1U, 0xE13EF6F4U, 0xE5FFEB43U, 0xE8BCCD9AU, 0xEC7DD02DU,
    0x34867077U, 0x30476DC0U, 0x3D044B19U, 0x39C556AEU, 0x278206ABU, 0x23431B1CU, 0x2E003DC5U, 0x2AC12072U,
    0x128E9DCFU, 0x164F8078U, 0x1B0CA6A1U, 0x1FCDBB16U, 0x018AEB13U, 0x054BF6A4U, 0x0808D07DU, 0x0CC9CDCAU,
    0x7897AB07U, 0x7C56B6B0U, 0x71159069U, 0x75D48DDEU, 0x6B93DDDBU, 0x6F52C06CU, 0x6211E6B5U, 0x66D0FB02U,
    0x5E9F46BFU, 0x5A5E5B08U, 0x571D7DD1U, 0x53DC6066U, 0x4D9B3063U, 0x495A2DD4U, 0x44190B0DU, 0x40D816BAU,
    0xACA5C697U, 0xA864DB20U, 0xA527FDF9U, 0xA1E6E04EU, 0xBFA1B04BU, 0xBB60ADFCU, 0xB6238B25U, 0xB2E29692U,
    0x8AAD2B2FU, 0x8E6C3698U, 0x832F1041U, 0x87EE0DF6U, 0x99A95DF3U, 0x9D684044U, 0x902B669DU, 0x94EA7B2AU,
    0xE0B41DE7U, 0xE4750050U, 0xE9362689U, 0xEDF73B3EU, 0xF3B06B3BU, 0xF771768CU, 0xFA325055U, 0xFEF34DE2U,
    0xC6BCF05FU, 0xC27DEDE8U, 0xCF3ECB31U, 0xCBFFD686U, 0xD5B88683U, 0xD1799B34U, 0xDC3ABDEDU, 0xD8FBA05AU,
    0x690CE0EEU, 0x6DCDFD59U, 0x608EDB80U, 0x644FC637U, 0x7A089632U, 0x7EC98B85U, 0x738AAD5CU, 0x774BB0EBU,
    0x4F040D56U, 0x4BC510E1U, 0x46863638U, 0x42472B8FU, 0x5C007B8AU, 0x58C1663DU, 0x558240E4U, 0x51435D53U,
    0x251D3B9EU, 0x21DC2629U, 0x2C9F00F0U, 0x285E1D47U, 0x36194D42U, 0x32D850F5U, 0x3F9B762CU, 0x3B5A6B9BU,
    0x0315D626U, 0x07D4CB91U, 0x0A97ED48U, 0x0E56F0FFU, 0x1011A0FAU, 0x14D0BD4DU, 0x19939B94U, 0x1D528623U,
    0xF12F560EU, 0xF5EE4BB9U, 0xF8AD6D60U, 0xFC6C70D7U, 0xE22B20D2U, 0xE6EA3D65U, 0xEBA91BBCU, 0xEF68060BU,
    0xD727BBB6U, 0xD3E6A601U, 0xDEA580D8U, 0xDA649D6FU, 0xC423CD6AU, 0xC0E2D0DDU, 0xCDA1F604U, 0xC960EBB3U,
    0xBD3E8D7EU, 0xB9FF90C9U, 0xB4BCB610U, 0xB07DABA7U, 0xAE3AFBA2U, 0xAAFBE615U, 0xA7B8C0CCU, 0xA379DD7BU,
    0x9B3660C6U, 0x9FF77D71U, 0x92B45BA8U, 0x9675461FU, 0x8832161AU, 0x8CF30BADU, 0x81B02D74U, 0x857130C3U,
    0x5D8A9099U, 0x594B8D2EU, 0x5408ABF7U, 0x50C9B640U, 0x4E8EE645U, 0x4A4FFBF2U, 0x470CDD2BU, 0x43CDC09CU,
    0x7B827D21U, 0x7F436096U, 0x7200464FU, 0x76C15BF8U, 0x68860BFDU, 0x6C47164AU, 0x61043093U, 0x65C52D24U,
    0x119B4BE9U, 0x155A565EU, 0x18197087U, 0x1CD86D30U, 0x029F3D35U, 0x065E2082U, 0x0B1D065BU, 0x0FDC1BECU,
    0x3793A651U, 0x3352BBE6U, 0x3E119D3FU, 0x3AD08088U, 0x2497D08DU, 0x2056CD3AU, 0x2D15EBE3U, 0x29D4F654U,
    0xC5A92679U, 0xC1683BCEU, 0xCC2B1D17U, 0xC8EA00A0U, 0xD6AD50A5U, 0xD26C4D12U, 0xDF2F6BCBU, 0xDBEE767CU,
    0xE3A1CBC1U, 0xE760D676U, 0xEA23F0AFU, 0xEEE2ED18U, 0xF0A5BD1DU, 0xF464A0AAU, 0xF9278673U, 0xFDE69BC4U,
    0x89B8FD09U, 0x8D79E0BEU, 0x803AC667U, 0x84FBDBD0U, 0x9ABC8BD5U, 0x9E7D9662U, 0x933EB0BBU, 0x97FFAD0CU,
    0xAFB010B1U, 0xAB710D06U, 0xA6322BDFU, 0xA2F33668U, 0xBCB4666DU, 0xB8757BDAU, 0xB5365D03U, 0xB1F740B4U
};

uint32_t crc32_bzip2_init(void) {
    return 0xFFFFFFFFU;
}

uint32_t crc32_bzip2_update(uint32_t state, const uint8_t *data, size_t len) {
    uint32_t crc = state;
    for (size_t i = 0; i < len; i++) {
        crc = crcglot_table_crc32_bzip2[((crc >> 24) ^ data[i]) & 0xFFU] ^ ((crc << 8) & 0xFFFFFFFFU);
    }
    return crc;
}

uint32_t crc32_bzip2_finalize(uint32_t state) {
    return state ^ 0xFFFFFFFFU;
}

uint32_t crc32_bzip2(const uint8_t *data, size_t len) {
    return crc32_bzip2_finalize(crc32_bzip2_update(crc32_bzip2_init(), data, len));
}

int crc32_bzip2_self_test(void) {
    static const uint8_t kCheckInput[] = "123456789";
    int failed = 0;
    if (crc32_bzip2(kCheckInput, 0) != 0x00000000U) {
        failed = 1;
    }
    if (crc32_bzip2(kCheckInput, 9) != 0xFC891918U) {
        failed = 1;
    }
    {
        uint32_t s = crc32_bzip2_init();
        for (unsigned int i = 0U; i < 256U; i++) {
            uint8_t b = (uint8_t)i;
            s = crc32_bzip2_update(s, &b, 1);
        }
        if (crc32_bzip2_finalize(s) != 0xB6B5EE95U) {
            failed = 1;
        }
    }
    {
        uint32_t s = crc32_bzip2_init();
        for (unsigned int i = 0U; i < 1024U; i++) {
            uint8_t b = (uint8_t)(((i * 167U) + 13U) & 0xFFU);
            s = crc32_bzip2_update(s, &b, 1);
        }
        if (crc32_bzip2_finalize(s) != 0x646333CAU) {
            failed = 1;
        }
    }
    return (failed != 0) ? 1 : 0;
}

#ifndef CRCGLOT_NO_PROVENANCE
const crcglot_provenance_t crc32_bzip2_provenance = {
    .version   = "0.30.0",
    .algorithm = "crc32-bzip2",
    .target    = "c",
    .variant   = "table",
    .comment   = "plain",
    .symbol    = "crc32_bzip2",
    .naming    = "snake",
};
#endif
```

</details>

<a id="variant-slice8"></a>

<details>
<summary>

### Slice-by-8

</summary>

_Eight tables; fastest, for width 32 / 64 on compiled targets._

```bash
crcglot c crc32-bzip2 --slice8
```

**`crc32_bzip2.h`**

```c
/* crc32-bzip2 -- bzip2 file compression, AAL5
 * Parameters: width=32, poly=0x04C11DB7, init=0xFFFFFFFF, refin=false, refout=false, xorout=0xFFFFFFFF.
 * check: crc32_bzip2(data, len) over "123456789" == 0xFC891918
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint32_t s = crc32_bzip2_init();
 *     s = crc32_bzip2_update(s, chunk, chunk_len);
 *     uint32_t crc = crc32_bzip2_finalize(s);
 *
 * One-shot: crc32_bzip2(data, len)
 * Verify:   crc32_bzip2_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc32-bzip2 -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc32-bzip2
 *     target:    c
 *     variant:   slice8
 *     comment:   plain
 *     symbol:    crc32_bzip2
 *     naming:    snake
 */
#ifndef CRC32_BZIP2_H
#define CRC32_BZIP2_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Return the initial CRC state to begin a computation.
 *
 * Returns the starting uint32_t state.
 */
uint32_t crc32_bzip2_init(void);

/* Fold input into the running CRC state and return the new state.
 *
 * state: running uint32_t state (from init or a prior update).
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the updated uint32_t state (not yet finalized).
 *
 * Call init -> update (any number of times) -> finalize.
 */
uint32_t crc32_bzip2_update(uint32_t state, const uint8_t *data, size_t len);

/* Apply the final XOR to produce the CRC.
 *
 * state: accumulated uint32_t state from update.
 *
 * Returns the finished CRC value.
 *
 * Do not feed the finalized value back into update.
 */
uint32_t crc32_bzip2_finalize(uint32_t state);

/* One-shot convenience: init + a single update + finalize.
 *
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the finished CRC value.
 */
uint32_t crc32_bzip2(const uint8_t *data, size_t len);

/* Self-test the implementation against independent reference CRCs.
 *
 * Returns 0 iff the generated CRC reproduces every embedded reference value.
 *
 * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 *
 * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 */
int crc32_bzip2_self_test(void);

#ifndef CRCGLOT_NO_PROVENANCE
#ifndef CRCGLOT_PROVENANCE_T_DEFINED
#define CRCGLOT_PROVENANCE_T_DEFINED
typedef struct {
    const char *version;
    const char *algorithm;
    const char *target;
    const char *variant;
    const char *comment;
    const char *symbol;
    const char *naming;
} crcglot_provenance_t;
#endif
extern const crcglot_provenance_t crc32_bzip2_provenance;
#endif

#ifdef __cplusplus
}
#endif

#endif /* CRC32_BZIP2_H */
```

**`crc32_bzip2.c`**

```c
/* crc32-bzip2 -- bzip2 file compression, AAL5
 * Parameters: width=32, poly=0x04C11DB7, init=0xFFFFFFFF, refin=false, refout=false, xorout=0xFFFFFFFF.
 * check: crc32_bzip2(data, len) over "123456789" == 0xFC891918
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint32_t s = crc32_bzip2_init();
 *     s = crc32_bzip2_update(s, chunk, chunk_len);
 *     uint32_t crc = crc32_bzip2_finalize(s);
 *
 * One-shot: crc32_bzip2(data, len)
 * Verify:   crc32_bzip2_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc32-bzip2 -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc32-bzip2
 *     target:    c
 *     variant:   slice8
 *     comment:   plain
 *     symbol:    crc32_bzip2
 *     naming:    snake
 */
#include "crc32_bzip2.h"

static const uint32_t crcglot_slice_crc32_bzip2[8][256] = {
    { /* T0 */
        0x00000000U, 0x04C11DB7U, 0x09823B6EU, 0x0D4326D9U, 0x130476DCU, 0x17C56B6BU, 0x1A864DB2U, 0x1E475005U,
        0x2608EDB8U, 0x22C9F00FU, 0x2F8AD6D6U, 0x2B4BCB61U, 0x350C9B64U, 0x31CD86D3U, 0x3C8EA00AU, 0x384FBDBDU,
        0x4C11DB70U, 0x48D0C6C7U, 0x4593E01EU, 0x4152FDA9U, 0x5F15ADACU, 0x5BD4B01BU, 0x569796C2U, 0x52568B75U,
        0x4C1C7E6EU, 0x17BDA2A4U, 0xFB5FC7FAU, 0xA0FE1B30U, 0x265A10F1U, 0x7DFBCC3BU, 0x9119A965U, 0xCAB875AFU,
        0x3548049BU, 0x6EE9D851U, 0x820BBD0FU, 0xD9AA61C5U, 0x5F0E6A04U, 0x04AFB6CEU, 0xE84DD390U, 0xB3EC0F5AU,
        0xE1C4D9A5U, 0xBA65056FU, 0x56876031U, 0x0D26BCFBU, 0x8B82B73AU, 0xD0236BF0U, 0x3CC10EAEU, 0x6760D264U
    }
};

uint32_t crc32_bzip2_init(void) {
    return 0xFFFFFFFFU;
}

uint32_t crc32_bzip2_update(uint32_t state, const uint8_t *data, size_t len) {
    uint32_t crc = state;
    while (len >= 8U) {
        uint32_t b03 = (uint32_t)data[0] << 24 | (uint32_t)data[1] << 16 | (uint32_t)data[2] << 8 | (uint32_t)data[3];
        uint32_t b47 = (uint32_t)data[4] << 24 | (uint32_t)data[5] << 16 | (uint32_t)data[6] << 8 | (uint32_t)data[7];
        uint32_t xored = crc ^ b03;
        crc = crcglot_slice_crc32_bzip2[7][(xored >> 24) & 0xFFU] ^ crcglot_slice_crc32_bzip2[6][(xored >> 16) & 0xFFU]
            ^ crcglot_slice_crc32_bzip2[5][(xored >>  8) & 0xFFU] ^ crcglot_slice_crc32_bzip2[4][ xored        & 0xFFU]
            ^ crcglot_slice_crc32_bzip2[3][(b47   >> 24) & 0xFFU] ^ crcglot_slice_crc32_bzip2[2][(b47   >> 16) & 0xFFU]
            ^ crcglot_slice_crc32_bzip2[1][(b47   >>  8) & 0xFFU] ^ crcglot_slice_crc32_bzip2[0][ b47          & 0xFFU];
        data += 8;
        len -= 8U;
    }
    while (len > 0U) {
        uint32_t top = crc >> 24;
        crc = crcglot_slice_crc32_bzip2[0][(top ^ *data) & 0xFFU] ^ (crc << 8);
        data++;
        len--;
    }
    return crc;
}

uint32_t crc32_bzip2_finalize(uint32_t state) {
    return state ^ 0xFFFFFFFFU;
}

uint32_t crc32_bzip2(const uint8_t *data, size_t len) {
    return crc32_bzip2_finalize(crc32_bzip2_update(crc32_bzip2_init(), data, len));
}

int crc32_bzip2_self_test(void) {
    static const uint8_t kCheckInput[] = "123456789";
    int failed = 0;
    if (crc32_bzip2(kCheckInput, 0) != 0x00000000U) {
        failed = 1;
    }
    if (crc32_bzip2(kCheckInput, 9) != 0xFC891918U) {
        failed = 1;
    }
    {
        uint32_t s = crc32_bzip2_init();
        for (unsigned int i = 0U; i < 256U; i++) {
            uint8_t b = (uint8_t)i;
            s = crc32_bzip2_update(s, &b, 1);
        }
        if (crc32_bzip2_finalize(s) != 0xB6B5EE95U) {
            failed = 1;
        }
    }
    {
        uint32_t s = crc32_bzip2_init();
        for (unsigned int i = 0U; i < 1024U; i++) {
            uint8_t b = (uint8_t)(((i * 167U) + 13U) & 0xFFU);
            s = crc32_bzip2_update(s, &b, 1);
        }
        if (crc32_bzip2_finalize(s) != 0x646333CAU) {
            failed = 1;
        }
    }
    return (failed != 0) ? 1 : 0;
}

#ifndef CRCGLOT_NO_PROVENANCE
const crcglot_provenance_t crc32_bzip2_provenance = {
    .version   = "0.30.0",
    .algorithm = "crc32-bzip2",
    .target    = "c",
    .variant   = "slice8",
    .comment   = "plain",
    .symbol    = "crc32_bzip2",
    .naming    = "snake",
};
#endif
```

</details>

## Documentation styles

The `--comment` flag picks the documentation style of the generated code; the algorithm and inner loop are identical.  Each style appears once, in a language that uses it, because seeing Doxygen three times says nothing the first block did not.  `plain`, professional comments in the language's native syntax, is the default and every target supports it.  Blocks below are `crc16-ibm-sdlc` bit-by-bit so you are reading comments rather than a lookup table.

<a id="style-c-plain"></a>

<details>
<summary>

### Plain (C / C++)

</summary>

_Human-readable comments in the language's native syntax_

```bash
crcglot c crc16-ibm-sdlc --small --comment plain
```

**`crc16_ibm_sdlc.h`**

```c
/* crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16_ibm_sdlc(data, len) over "123456789" == 0x906E
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint16_t s = crc16_ibm_sdlc_init();
 *     s = crc16_ibm_sdlc_update(s, chunk, chunk_len);
 *     uint16_t crc = crc16_ibm_sdlc_finalize(s);
 *
 * One-shot: crc16_ibm_sdlc(data, len)
 * Verify:   crc16_ibm_sdlc_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    c
 *     variant:   bitwise
 *     comment:   plain
 *     symbol:    crc16_ibm_sdlc
 *     naming:    snake
 */
#ifndef CRC16_IBM_SDLC_H
#define CRC16_IBM_SDLC_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Return the initial CRC state to begin a computation.
 *
 * Returns the starting uint16_t state.
 */
uint16_t crc16_ibm_sdlc_init(void);

/* Fold input into the running CRC state and return the new state.
 *
 * state: running uint16_t state (from init or a prior update).
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the updated uint16_t state (not yet finalized).
 *
 * Call init -> update (any number of times) -> finalize.
 */
uint16_t crc16_ibm_sdlc_update(uint16_t state, const uint8_t *data, size_t len);

/* Apply the final XOR to produce the CRC.
 *
 * state: accumulated uint16_t state from update.
 *
 * Returns the finished CRC value.
 *
 * Do not feed the finalized value back into update.
 */
uint16_t crc16_ibm_sdlc_finalize(uint16_t state);

/* One-shot convenience: init + a single update + finalize.
 *
 * data: pointer to the message bytes.
 * len: number of bytes available at data.
 *
 * Returns the finished CRC value.
 */
uint16_t crc16_ibm_sdlc(const uint8_t *data, size_t len);

/* Self-test the implementation against independent reference CRCs.
 *
 * Returns 0 iff the generated CRC reproduces every embedded reference value.
 *
 * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 *
 * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 */
int crc16_ibm_sdlc_self_test(void);

#ifndef CRCGLOT_NO_PROVENANCE
#ifndef CRCGLOT_PROVENANCE_T_DEFINED
#define CRCGLOT_PROVENANCE_T_DEFINED
typedef struct {
    const char *version;
    const char *algorithm;
    const char *target;
    const char *variant;
    const char *comment;
    const char *symbol;
    const char *naming;
} crcglot_provenance_t;
#endif
extern const crcglot_provenance_t crc16_ibm_sdlc_provenance;
#endif

#ifdef __cplusplus
}
#endif

#endif /* CRC16_IBM_SDLC_H */
```

**`crc16_ibm_sdlc.c`**

```c
/* crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16_ibm_sdlc(data, len) over "123456789" == 0x906E
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     uint16_t s = crc16_ibm_sdlc_init();
 *     s = crc16_ibm_sdlc_update(s, chunk, chunk_len);
 *     uint16_t crc = crc16_ibm_sdlc_finalize(s);
 *
 * One-shot: crc16_ibm_sdlc(data, len)
 * Verify:   crc16_ibm_sdlc_self_test() returns 0 on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    c
 *     variant:   bitwise
 *     comment:   plain
 *     symbol:    crc16_ibm_sdlc
 *     naming:    snake
 */
#include "crc16_ibm_sdlc.h"

uint16_t crc16_ibm_sdlc_init(void) {
    return 0xFFFFU;
}

uint16_t crc16_ibm_sdlc_update(uint16_t state, const uint8_t *data, size_t len) {
    uint16_t crc = state;
    for (size_t i = 0; i < len; i++) {
        crc ^= data[i];
        for (int j = 0; j < 8; j++) {
            if ((crc & 1U) != 0U) {
                crc = (crc >> 1) ^ 0x8408U;
            } else {
                crc >>= 1;
            }
        }
    }
    return crc;
}

uint16_t crc16_ibm_sdlc_finalize(uint16_t state) {
    return state ^ 0xFFFFU;
}

uint16_t crc16_ibm_sdlc(const uint8_t *data, size_t len) {
    return crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_update(crc16_ibm_sdlc_init(), data, len));
}

int crc16_ibm_sdlc_self_test(void) {
    static const uint8_t kCheckInput[] = "123456789";
    int failed = 0;
    if (crc16_ibm_sdlc(kCheckInput, 0) != 0x0000U) {
        failed = 1;
    }
    if (crc16_ibm_sdlc(kCheckInput, 9) != 0x906EU) {
        failed = 1;
    }
    {
        uint16_t s = crc16_ibm_sdlc_init();
        for (unsigned int i = 0U; i < 256U; i++) {
            uint8_t b = (uint8_t)i;
            s = crc16_ibm_sdlc_update(s, &b, 1);
        }
        if (crc16_ibm_sdlc_finalize(s) != 0x303CU) {
            failed = 1;
        }
    }
    {
        uint16_t s = crc16_ibm_sdlc_init();
        for (unsigned int i = 0U; i < 1024U; i++) {
            uint8_t b = (uint8_t)(((i * 167U) + 13U) & 0xFFU);
            s = crc16_ibm_sdlc_update(s, &b, 1);
        }
        if (crc16_ibm_sdlc_finalize(s) != 0x8643U) {
            failed = 1;
        }
    }
    return (failed != 0) ? 1 : 0;
}

#ifndef CRCGLOT_NO_PROVENANCE
const crcglot_provenance_t crc16_ibm_sdlc_provenance = {
    .version   = "0.30.0",
    .algorithm = "crc16-ibm-sdlc",
    .target    = "c",
    .variant   = "bitwise",
    .comment   = "plain",
    .symbol    = "crc16_ibm_sdlc",
    .naming    = "snake",
};
#endif
```

</details>

<a id="style-c-doxygen"></a>

<details>
<summary>

### Doxygen (C / C++)

</summary>

_Doxygen /** @brief @param @return */ markup_

```bash
crcglot c crc16-ibm-sdlc --small --comment doxygen
```

**`crc16_ibm_sdlc.h`**

```c
/** @file
 * @brief crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 *
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16_ibm_sdlc(data, len) over "123456789" == 0x906E
 *
 * Streaming -- call init, then update over each chunk, then finalize:
 * @code
 *     uint16_t s = crc16_ibm_sdlc_init();
 *     s = crc16_ibm_sdlc_update(s, chunk, chunk_len);
 *     uint16_t crc = crc16_ibm_sdlc_finalize(s);
 * @endcode
 *
 * One-shot: crc16_ibm_sdlc(data, len)
 * @note Verify with crc16_ibm_sdlc_self_test() (returns 0 on success); run it once on your toolchain to catch a compiler / endianness mismatch.
 *
 * @note Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    c
 *     variant:   bitwise
 *     comment:   doxygen
 *     symbol:    crc16_ibm_sdlc
 *     naming:    snake
 */
#ifndef CRC16_IBM_SDLC_H
#define CRC16_IBM_SDLC_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

/** @brief Return the initial CRC state to begin a computation.
 * @return the starting uint16_t state.
 */
uint16_t crc16_ibm_sdlc_init(void);

/** @brief Fold input into the running CRC state and return the new state.
 *
 * @param state running uint16_t state (from init or a prior update).
 * @param data pointer to the message bytes.
 * @param len number of bytes available at data.
 * @return the updated uint16_t state (not yet finalized).
 * @note Call init -> update (any number of times) -> finalize.
 */
uint16_t crc16_ibm_sdlc_update(uint16_t state, const uint8_t *data, size_t len);

/** @brief Apply the final XOR to produce the CRC.
 *
 * @param state accumulated uint16_t state from update.
 * @return the finished CRC value.
 * @note Do not feed the finalized value back into update.
 */
uint16_t crc16_ibm_sdlc_finalize(uint16_t state);

/** @brief One-shot convenience: init + a single update + finalize.
 *
 * @param data pointer to the message bytes.
 * @param len number of bytes available at data.
 * @return the finished CRC value.
 */
uint16_t crc16_ibm_sdlc(const uint8_t *data, size_t len);

/** @brief Self-test the implementation against independent reference CRCs.
 * @return 0 iff the generated CRC reproduces every embedded reference value.
 * @note Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 * @note Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 */
int crc16_ibm_sdlc_self_test(void);

#ifndef CRCGLOT_NO_PROVENANCE
#ifndef CRCGLOT_PROVENANCE_T_DEFINED
#define CRCGLOT_PROVENANCE_T_DEFINED
typedef struct {
    const char *version;
    const char *algorithm;
    const char *target;
    const char *variant;
    const char *comment;
    const char *symbol;
    const char *naming;
} crcglot_provenance_t;
#endif
extern const crcglot_provenance_t crc16_ibm_sdlc_provenance;
#endif

#ifdef __cplusplus
}
#endif

#endif /* CRC16_IBM_SDLC_H */
```

**`crc16_ibm_sdlc.c`**

```c
/** @file
 * @brief crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 *
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16_ibm_sdlc(data, len) over "123456789" == 0x906E
 *
 * Streaming -- call init, then update over each chunk, then finalize:
 * @code
 *     uint16_t s = crc16_ibm_sdlc_init();
 *     s = crc16_ibm_sdlc_update(s, chunk, chunk_len);
 *     uint16_t crc = crc16_ibm_sdlc_finalize(s);
 * @endcode
 *
 * One-shot: crc16_ibm_sdlc(data, len)
 * @note Verify with crc16_ibm_sdlc_self_test() (returns 0 on success); run it once on your toolchain to catch a compiler / endianness mismatch.
 *
 * @note Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    c
 *     variant:   bitwise
 *     comment:   doxygen
 *     symbol:    crc16_ibm_sdlc
 *     naming:    snake
 */
#include "crc16_ibm_sdlc.h"

uint16_t crc16_ibm_sdlc_init(void) {
    return 0xFFFFU;
}

uint16_t crc16_ibm_sdlc_update(uint16_t state, const uint8_t *data, size_t len) {
    uint16_t crc = state;
    for (size_t i = 0; i < len; i++) {
        crc ^= data[i];
        for (int j = 0; j < 8; j++) {
            if ((crc & 1U) != 0U) {
                crc = (crc >> 1) ^ 0x8408U;
            } else {
                crc >>= 1;
            }
        }
    }
    return crc;
}

uint16_t crc16_ibm_sdlc_finalize(uint16_t state) {
    return state ^ 0xFFFFU;
}

uint16_t crc16_ibm_sdlc(const uint8_t *data, size_t len) {
    return crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_update(crc16_ibm_sdlc_init(), data, len));
}

int crc16_ibm_sdlc_self_test(void) {
    static const uint8_t kCheckInput[] = "123456789";
    int failed = 0;
    if (crc16_ibm_sdlc(kCheckInput, 0) != 0x0000U) {
        failed = 1;
    }
    if (crc16_ibm_sdlc(kCheckInput, 9) != 0x906EU) {
        failed = 1;
    }
    {
        uint16_t s = crc16_ibm_sdlc_init();
        for (unsigned int i = 0U; i < 256U; i++) {
            uint8_t b = (uint8_t)i;
            s = crc16_ibm_sdlc_update(s, &b, 1);
        }
        if (crc16_ibm_sdlc_finalize(s) != 0x303CU) {
            failed = 1;
        }
    }
    {
        uint16_t s = crc16_ibm_sdlc_init();
        for (unsigned int i = 0U; i < 1024U; i++) {
            uint8_t b = (uint8_t)(((i * 167U) + 13U) & 0xFFU);
            s = crc16_ibm_sdlc_update(s, &b, 1);
        }
        if (crc16_ibm_sdlc_finalize(s) != 0x8643U) {
            failed = 1;
        }
    }
    return (failed != 0) ? 1 : 0;
}

#ifndef CRCGLOT_NO_PROVENANCE
const crcglot_provenance_t crc16_ibm_sdlc_provenance = {
    .version   = "0.30.0",
    .algorithm = "crc16-ibm-sdlc",
    .target    = "c",
    .variant   = "bitwise",
    .comment   = "doxygen",
    .symbol    = "crc16_ibm_sdlc",
    .naming    = "snake",
};
#endif
```

</details>

<a id="style-csharp-docfx"></a>

<details>
<summary>

### DocFX (C#)

</summary>

_C# XML doc comments (/// <summary> <param> <returns>)_

```bash
crcglot csharp crc16-ibm-sdlc --small --comment docfx
```

```csharp
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: Crc16IbmSdlc.Compute(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     ushort s = Crc16IbmSdlc.Init();
//     s = Crc16IbmSdlc.Update(s, chunk);  // over each chunk
//     ushort crc = Crc16IbmSdlc.Finalize(s);
//
// One-shot: Crc16IbmSdlc.Compute(data)
// Verify:   Crc16IbmSdlc.SelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    csharp
//     variant:   bitwise
//     comment:   docfx
//     symbol:    crc16_ibm_sdlc
//     naming:    pascal

using System;

public static class Crc16IbmSdlc
{
    /// <summary>
    /// Return the initial CRC state to begin a computation.
    /// </summary>
    /// <returns>the starting ushort state.</returns>
    public static ushort Init() {
        return 0xFFFF;
    }

    /// <summary>
    /// Fold input into the running CRC state and return the new state.
    /// </summary>
    /// <param name="state">running ushort state (from init or a prior update).</param>
    /// <param name="data">the message bytes.</param>
    /// <returns>the updated ushort state (not yet finalized).</returns>
    /// <remarks>
    /// Call init -> update (any number of times) -> finalize.
    /// </remarks>
    public static ushort Update(ushort state, byte[] data) {
        ushort crc = state;
        foreach (byte b in data) {
            crc ^= b;
            for (int i = 0; i < 8; i++) {
                if ((crc & 0x0001) != 0)
                    crc = (ushort)((crc >> 1) ^ 0x8408);
                else
                    crc >>= 1;
            }
        }
        return crc;
    }

    /// <summary>
    /// Apply the final XOR to produce the CRC.
    /// </summary>
    /// <param name="state">accumulated ushort state from update.</param>
    /// <returns>the finished CRC value.</returns>
    /// <remarks>
    /// Do not feed the finalized value back into update.
    /// </remarks>
    public static ushort Finalize(ushort state) {
        return (ushort)(state ^ 0xFFFF);
    }

    /// <summary>
    /// One-shot convenience: init + a single update + finalize.
    /// </summary>
    /// <param name="data">the message bytes.</param>
    /// <returns>the finished CRC value.</returns>
    public static ushort Compute(byte[] data) {
        return Finalize(Update(Init(), data));
    }

    /// <summary>
    /// Self-test the implementation against independent reference CRCs.
    /// </summary>
    /// <returns>true iff the generated CRC reproduces every embedded reference value.</returns>
    /// <remarks>
    /// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
    /// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    /// </remarks>
    public static bool SelfTest() {
        if (Compute(new byte[0]) != 0x0000) return false;
        if (Compute(new byte[] { 0x31, 0x32, 0x33, 0x34, 0x35, 0x36, 0x37, 0x38, 0x39 }) != 0x906E) return false;
        {
            ushort s = Init();
            for (int i = 0; i < 256; i++) {
                s = Update(s, new byte[] { (byte)i });
            }
            if (Finalize(s) != 0x303C) return false;
        }
        {
            ushort s = Init();
            for (int i = 0; i < 1024; i++) {
                s = Update(s, new byte[] { (byte)((i * 167 + 13) & 0xFF) });
            }
            if (Finalize(s) != 0x8643) return false;
        }
        return true;
    }
}
```

</details>

<a id="style-go-godoc"></a>

<details>
<summary>

### Godoc (Go)

</summary>

_godoc // comments that open with the identifier_

```bash
crcglot go crc16-ibm-sdlc --small --comment godoc
```

```go
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: Crc16IbmSdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     s := Crc16IbmSdlcInit()
//     s = Crc16IbmSdlcUpdate(s, chunk)  // over each chunk of the message
//     crc := Crc16IbmSdlcFinalize(s)
//
// One-shot: Crc16IbmSdlc(data)
// Verify:   Crc16IbmSdlcSelfTest() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    go
//     variant:   bitwise
//     comment:   godoc
//     symbol:    crc16_ibm_sdlc
//     naming:    pascal

package crc

// Crc16IbmSdlcInit return the initial CRC state to begin a computation.
//
// Returns the starting uint16 state.
func Crc16IbmSdlcInit() uint16 {
    return 0xFFFF
}

// Crc16IbmSdlcUpdate fold input into the running CRC state and return the new state.
//
// state: running uint16 state (from init or a prior update).
// data: the message bytes.
//
// Returns the updated uint16 state (not yet finalized).
//
// Call init -> update (any number of times) -> finalize.
func Crc16IbmSdlcUpdate(state uint16, data []byte) uint16 {
    crc := state
    for _, b := range data {
        crc ^= uint16(b)
        for i := 0; i < 8; i++ {
            if crc&1 != 0 {
                crc = (crc >> 1) ^ 0x8408
            } else {
                crc >>= 1
            }
        }
    }
    return crc
}

// Crc16IbmSdlcFinalize apply the final XOR to produce the CRC.
//
// state: accumulated uint16 state from update.
//
// Returns the finished CRC value.
//
// Do not feed the finalized value back into update.
func Crc16IbmSdlcFinalize(state uint16) uint16 {
    return state ^ 0xFFFF
}

// Crc16IbmSdlc one-shot convenience: init + a single update + finalize.
//
// data: the message bytes.
//
// Returns the finished CRC value.
func Crc16IbmSdlc(data []byte) uint16 {
    return Crc16IbmSdlcFinalize(Crc16IbmSdlcUpdate(Crc16IbmSdlcInit(), data))
}

// Crc16IbmSdlcSelfTest self-test the implementation against independent reference CRCs.
//
// Returns true iff the generated CRC reproduces every embedded reference value.
//
// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
//
// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
func Crc16IbmSdlcSelfTest() bool {
    if Crc16IbmSdlc([]byte("")) != 0x0000 {
        return false
    }
    if Crc16IbmSdlc([]byte("123456789")) != 0x906E {
        return false
    }
    s := Crc16IbmSdlcInit()
    for i := 0; i < 256; i++ {
        s = Crc16IbmSdlcUpdate(s, []byte{byte(i)})
    }
    if Crc16IbmSdlcFinalize(s) != 0x303C {
        return false
    }
    s = Crc16IbmSdlcInit()
    for i := 0; i < 1024; i++ {
        s = Crc16IbmSdlcUpdate(s, []byte{byte((i*167 + 13) & 0xFF)})
    }
    if Crc16IbmSdlcFinalize(s) != 0x8643 {
        return false
    }
    return true
}
```

</details>

<a id="style-java-javadoc"></a>

<details>
<summary>

### Javadoc (Java)

</summary>

_JavaDoc /** @param @return */ comments_

```bash
crcglot java crc16-ibm-sdlc --small --comment javadoc
```

```java
/** crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: Crc16IbmSdlc.crc16IbmSdlc(data) over "123456789" == 0x906E
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     int s = Crc16IbmSdlc.crc16IbmSdlcInit();
 *     s = Crc16IbmSdlc.crc16IbmSdlcUpdate(s, chunk);  // over each chunk
 *     int crc = Crc16IbmSdlc.crc16IbmSdlcFinalize(s);
 *
 * One-shot: Crc16IbmSdlc.crc16IbmSdlc(data)
 * Verify:   Crc16IbmSdlc.crc16IbmSdlcSelfTest() returns true on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    java
 *     variant:   bitwise
 *     comment:   javadoc
 *     symbol:    crc16_ibm_sdlc
 *     naming:    camel
 */

public final class Crc16IbmSdlc {
    /** Return the initial CRC state to begin a computation.
     * @return the starting int state.
     */
    public static int crc16IbmSdlcInit() {
        return 0xFFFF;
    }

    /** Fold input into the running CRC state and return the new state.
     *
     * Call init -> update (any number of times) -> finalize.
     *
     * @param state running int state (from init or a prior update).
     * @param data the message bytes.
     * @return the updated int state (not yet finalized).
     */
    public static int crc16IbmSdlcUpdate(int state, byte[] data) {
        int crc = state;
        for (byte b : data) {
            crc ^= (b & 0xFF);
            for (int i = 0; i < 8; i++) {
                if ((crc & 1) != 0)
                    crc = (crc >>> 1) ^ 0x8408;
                else
                    crc >>>= 1;
            }
        }
        return crc;
    }

    /** Apply the final XOR to produce the CRC.
     *
     * Do not feed the finalized value back into update.
     *
     * @param state accumulated int state from update.
     * @return the finished CRC value.
     */
    public static int crc16IbmSdlcFinalize(int state) {
        return state ^ 0xFFFF;
    }

    /** One-shot convenience: init + a single update + finalize.
     *
     * @param data the message bytes.
     * @return the finished CRC value.
     */
    public static int crc16IbmSdlc(byte[] data) {
        return crc16IbmSdlcFinalize(crc16IbmSdlcUpdate(crc16IbmSdlcInit(), data));
    }

    /** Self-test the implementation against independent reference CRCs.
     *
     * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
     *
     * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
     * @return true iff the generated CRC reproduces every embedded reference value.
     */
    public static boolean crc16IbmSdlcSelfTest() {
        if (crc16IbmSdlc(new byte[0]) != 0x0000) return false;
        if (crc16IbmSdlc(new byte[] { 0x31, 0x32, 0x33, 0x34, 0x35, 0x36, 0x37, 0x38, 0x39 }) != 0x906E) return false;
        {
            int s = crc16IbmSdlcInit();
            for (int i = 0; i < 256; i++) {
                s = crc16IbmSdlcUpdate(s, new byte[] { (byte) i });
            }
            if (crc16IbmSdlcFinalize(s) != 0x303C) return false;
        }
        {
            int s = crc16IbmSdlcInit();
            for (int i = 0; i < 1024; i++) {
                s = crc16IbmSdlcUpdate(s, new byte[] { (byte) ((i * 167 + 13) & 0xFF) });
            }
            if (crc16IbmSdlcFinalize(s) != 0x8643) return false;
        }
        return true;
    }
}

```

</details>

<a id="style-python-google"></a>

<details>
<summary>

### Google (Python)

</summary>

_Google-style docstrings (Args / Returns / Note)_

```bash
crcglot python crc16-ibm-sdlc --small --comment google
```

```python
"""crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS

Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
check: crc16_ibm_sdlc(data) over "123456789" == 0x906E

Example:
    Streaming -- call init, then update over each chunk, then finalize::

        s = crc16_ibm_sdlc_init()
        s = crc16_ibm_sdlc_update(s, chunk)  # over each chunk of the message
        crc = crc16_ibm_sdlc_finalize(s)

    One-shot: crc16_ibm_sdlc(data)

Note:
    Verify with crc16_ibm_sdlc_self_test() (returns True on success); run it once on your interpreter to catch a build / width mismatch.
    Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
    Reproduce with crcglot:
        version:   0.30.0
        algorithm: crc16-ibm-sdlc
        target:    python
        variant:   bitwise
        comment:   google
        symbol:    crc16_ibm_sdlc
        naming:    snake
"""


def crc16_ibm_sdlc_init() -> int:
    """Return the initial CRC state to begin a computation.

    Returns:
        the starting int state.
    """
    return 0xFFFF


def crc16_ibm_sdlc_update(state: int, data: bytes) -> int:
    """Fold input into the running CRC state and return the new state.

    Args:
        state: running int state (from init or a prior update).
        data: the message bytes (a bytes-like object).

    Returns:
        the updated int state (not yet finalized).

    Note:
        Call init -> update (any number of times) -> finalize.
    """
    crc = state
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 1:
                crc = (crc >> 1) ^ 0x8408
            else:
                crc >>= 1
    return crc


def crc16_ibm_sdlc_finalize(state: int) -> int:
    """Apply the final XOR to produce the CRC.

    Args:
        state: accumulated int state from update.

    Returns:
        the finished CRC value.

    Note:
        Do not feed the finalized value back into update.
    """
    return state ^ 0xFFFF


def crc16_ibm_sdlc(data: bytes) -> int:
    """One-shot convenience: init + a single update + finalize.

    Args:
        data: the message bytes (a bytes-like object).

    Returns:
        the finished CRC value.
    """
    state = crc16_ibm_sdlc_init()
    state = crc16_ibm_sdlc_update(state, data)
    return crc16_ibm_sdlc_finalize(state)


def crc16_ibm_sdlc_self_test() -> bool:
    """Self-test the implementation against independent reference CRCs.

    Returns:
        True iff the generated CRC reproduces every embedded reference value.

    Note:
        Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
        Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    """
    if crc16_ibm_sdlc(b"") != 0x0000:
        return False
    if crc16_ibm_sdlc(b"123456789") != 0x906E:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(256):
        s = crc16_ibm_sdlc_update(s, bytes([i]))
    if crc16_ibm_sdlc_finalize(s) != 0x303C:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(1024):
        s = crc16_ibm_sdlc_update(s, bytes([(i * 167 + 13) & 0xFF]))
    if crc16_ibm_sdlc_finalize(s) != 0x8643:
        return False
    return True
```

</details>

<a id="style-python-numpy"></a>

<details>
<summary>

### NumPy (Python)

</summary>

_NumPy (numpydoc) docstrings, underlined Parameters / Returns_

```bash
crcglot python crc16-ibm-sdlc --small --comment numpy
```

```python
"""crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
check: crc16_ibm_sdlc(data) over "123456789" == 0x906E

Streaming (init -> update -> finalize); update may be called any
number of times over successive chunks of the message:

    s = crc16_ibm_sdlc_init()
    s = crc16_ibm_sdlc_update(s, chunk)  # over each chunk of the message
    crc = crc16_ibm_sdlc_finalize(s)

One-shot: crc16_ibm_sdlc(data)
Verify:   crc16_ibm_sdlc_self_test() returns True on success; run it once on your
          toolchain to catch a compiler / endianness mismatch.

Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
Reproduce with crcglot:
    version:   0.30.0
    algorithm: crc16-ibm-sdlc
    target:    python
    variant:   bitwise
    comment:   numpy
    symbol:    crc16_ibm_sdlc
    naming:    snake
"""


def crc16_ibm_sdlc_init() -> int:
    """Return the initial CRC state to begin a computation.

    Returns
    -------
        the starting int state.
    """
    return 0xFFFF


def crc16_ibm_sdlc_update(state: int, data: bytes) -> int:
    """Fold input into the running CRC state and return the new state.

    Parameters
    ----------
    state
        running int state (from init or a prior update).
    data
        the message bytes (a bytes-like object).

    Returns
    -------
        the updated int state (not yet finalized).

    Notes
    -----
    Call init -> update (any number of times) -> finalize.
    """
    crc = state
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 1:
                crc = (crc >> 1) ^ 0x8408
            else:
                crc >>= 1
    return crc


def crc16_ibm_sdlc_finalize(state: int) -> int:
    """Apply the final XOR to produce the CRC.

    Parameters
    ----------
    state
        accumulated int state from update.

    Returns
    -------
        the finished CRC value.

    Notes
    -----
    Do not feed the finalized value back into update.
    """
    return state ^ 0xFFFF


def crc16_ibm_sdlc(data: bytes) -> int:
    """One-shot convenience: init + a single update + finalize.

    Parameters
    ----------
    data
        the message bytes (a bytes-like object).

    Returns
    -------
        the finished CRC value.
    """
    state = crc16_ibm_sdlc_init()
    state = crc16_ibm_sdlc_update(state, data)
    return crc16_ibm_sdlc_finalize(state)


def crc16_ibm_sdlc_self_test() -> bool:
    """Self-test the implementation against independent reference CRCs.

    Returns
    -------
        True iff the generated CRC reproduces every embedded reference value.

    Notes
    -----
    Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
    Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    """
    if crc16_ibm_sdlc(b"") != 0x0000:
        return False
    if crc16_ibm_sdlc(b"123456789") != 0x906E:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(256):
        s = crc16_ibm_sdlc_update(s, bytes([i]))
    if crc16_ibm_sdlc_finalize(s) != 0x303C:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(1024):
        s = crc16_ibm_sdlc_update(s, bytes([(i * 167 + 13) & 0xFF]))
    if crc16_ibm_sdlc_finalize(s) != 0x8643:
        return False
    return True
```

</details>

<a id="style-python-rest"></a>

<details>
<summary>

### reStructuredText (Python)

</summary>

_Sphinx field-list docstrings (:param: / :returns:)_

```bash
crcglot python crc16-ibm-sdlc --small --comment rest
```

```python
"""crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
check: crc16_ibm_sdlc(data) over "123456789" == 0x906E

Streaming (init -> update -> finalize); update may be called any
number of times over successive chunks of the message:

    s = crc16_ibm_sdlc_init()
    s = crc16_ibm_sdlc_update(s, chunk)  # over each chunk of the message
    crc = crc16_ibm_sdlc_finalize(s)

One-shot: crc16_ibm_sdlc(data)
Verify:   crc16_ibm_sdlc_self_test() returns True on success; run it once on your
          toolchain to catch a compiler / endianness mismatch.

Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
Reproduce with crcglot:
    version:   0.30.0
    algorithm: crc16-ibm-sdlc
    target:    python
    variant:   bitwise
    comment:   rest
    symbol:    crc16_ibm_sdlc
    naming:    snake
"""


def crc16_ibm_sdlc_init() -> int:
    """Return the initial CRC state to begin a computation.

    :returns: the starting int state.
    """
    return 0xFFFF


def crc16_ibm_sdlc_update(state: int, data: bytes) -> int:
    """Fold input into the running CRC state and return the new state.

    :param state: running int state (from init or a prior update).
    :param data: the message bytes (a bytes-like object).
    :returns: the updated int state (not yet finalized).

    .. note::
       Call init -> update (any number of times) -> finalize.
    """
    crc = state
    for byte in data:
        crc ^= byte
        for _ in range(8):
            if crc & 1:
                crc = (crc >> 1) ^ 0x8408
            else:
                crc >>= 1
    return crc


def crc16_ibm_sdlc_finalize(state: int) -> int:
    """Apply the final XOR to produce the CRC.

    :param state: accumulated int state from update.
    :returns: the finished CRC value.

    .. note::
       Do not feed the finalized value back into update.
    """
    return state ^ 0xFFFF


def crc16_ibm_sdlc(data: bytes) -> int:
    """One-shot convenience: init + a single update + finalize.

    :param data: the message bytes (a bytes-like object).
    :returns: the finished CRC value.
    """
    state = crc16_ibm_sdlc_init()
    state = crc16_ibm_sdlc_update(state, data)
    return crc16_ibm_sdlc_finalize(state)


def crc16_ibm_sdlc_self_test() -> bool:
    """Self-test the implementation against independent reference CRCs.

    :returns: True iff the generated CRC reproduces every embedded reference value.

    .. note::
       Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.

    .. note::
       Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
    """
    if crc16_ibm_sdlc(b"") != 0x0000:
        return False
    if crc16_ibm_sdlc(b"123456789") != 0x906E:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(256):
        s = crc16_ibm_sdlc_update(s, bytes([i]))
    if crc16_ibm_sdlc_finalize(s) != 0x303C:
        return False
    s = crc16_ibm_sdlc_init()
    for i in range(1024):
        s = crc16_ibm_sdlc_update(s, bytes([(i * 167 + 13) & 0xFF]))
    if crc16_ibm_sdlc_finalize(s) != 0x8643:
        return False
    return True
```

</details>

<a id="style-rust-rustdoc"></a>

<details>
<summary>

### Rustdoc (Rust)

</summary>

_rustdoc /// docs with Markdown (# Arguments / # Returns)_

```bash
crcglot rust crc16-ibm-sdlc --small --comment rustdoc
```

```rust
// crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
// Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
// check: crc16_ibm_sdlc(data) over "123456789" == 0x906E
//
// Streaming (init -> update -> finalize); update may be called any
// number of times over successive chunks of the message:
//
//     let s = crc16_ibm_sdlc_init();
//     let s = crc16_ibm_sdlc_update(s, chunk);  // over each chunk
//     let crc = crc16_ibm_sdlc_finalize(s);
//
// One-shot: crc16_ibm_sdlc(data)
// Verify:   crc16_ibm_sdlc_self_test() returns true on success; run it once on your
//           toolchain to catch a compiler / endianness mismatch.
//
// Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
// Reproduce with crcglot:
//     version:   0.30.0
//     algorithm: crc16-ibm-sdlc
//     target:    rust
//     variant:   bitwise
//     comment:   rustdoc
//     symbol:    crc16_ibm_sdlc
//     naming:    snake

/// Return the initial CRC state to begin a computation.
///
/// # Returns
///
/// The starting u16 state.
pub fn crc16_ibm_sdlc_init() -> u16 {
    0xFFFF
}

/// Fold input into the running CRC state and return the new state.
///
/// # Arguments
///
/// * `state` - running u16 state (from init or a prior update).
/// * `data` - the message bytes.
///
/// # Returns
///
/// The updated u16 state (not yet finalized).
///
/// Call init -> update (any number of times) -> finalize.
pub fn crc16_ibm_sdlc_update(state: u16, data: &[u8]) -> u16 {
    let mut crc: u16 = state;
    for &byte in data {
        crc ^= byte as u16;
        for _ in 0..8 {
            if crc & 1 != 0 {
                crc = (crc >> 1) ^ 0x8408;
            } else {
                crc >>= 1;
            }
        }
    }
    crc
}

/// Apply the final XOR to produce the CRC.
///
/// # Arguments
///
/// * `state` - accumulated u16 state from update.
///
/// # Returns
///
/// The finished CRC value.
///
/// Do not feed the finalized value back into update.
pub fn crc16_ibm_sdlc_finalize(state: u16) -> u16 {
    state ^ 0xFFFF
}

/// One-shot convenience: init + a single update + finalize.
///
/// # Arguments
///
/// * `data` - the message bytes.
///
/// # Returns
///
/// The finished CRC value.
pub fn crc16_ibm_sdlc(data: &[u8]) -> u16 {
    crc16_ibm_sdlc_finalize(crc16_ibm_sdlc_update(crc16_ibm_sdlc_init(), data))
}

/// Self-test the implementation against independent reference CRCs.
///
/// # Returns
///
/// True iff the generated CRC reproduces every embedded reference value.
///
/// Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
///
/// Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
pub fn crc16_ibm_sdlc_self_test() -> bool {
    if crc16_ibm_sdlc(b"") != 0x0000_u16 {
        return false;
    }
    if crc16_ibm_sdlc(b"123456789") != 0x906E_u16 {
        return false;
    }
    let mut s = crc16_ibm_sdlc_init();
    for i in 0u32..256 {
        s = crc16_ibm_sdlc_update(s, &[i as u8]);
    }
    if crc16_ibm_sdlc_finalize(s) != 0x303C_u16 {
        return false;
    }
    s = crc16_ibm_sdlc_init();
    for i in 0u32..1024 {
        s = crc16_ibm_sdlc_update(s, &[((i * 167 + 13) & 0xFF) as u8]);
    }
    if crc16_ibm_sdlc_finalize(s) != 0x8643_u16 {
        return false;
    }
    true
}
```

</details>

<a id="style-typescript-jsdoc"></a>

<details>
<summary>

### JSDoc (TypeScript)

</summary>

_TSDoc /** @param x - ... @returns */ comments_

```bash
crcglot typescript crc16-ibm-sdlc --small --comment jsdoc
```

```typescript
/** crc16-ibm-sdlc -- IBM SDLC, ISO HDLC, X.25 FCS
 * Parameters: width=16, poly=0x1021, init=0xFFFF, refin=true, refout=true, xorout=0xFFFF.
 * check: crc16IbmSdlc(data) over "123456789" == 0x906E
 *
 * Streaming (init -> update -> finalize); update may be called any
 * number of times over successive chunks of the message:
 *
 *     let s = crc16IbmSdlcInit();
 *     s = crc16IbmSdlcUpdate(s, chunk);  // over each chunk of the message
 *     const crc = crc16IbmSdlcFinalize(s);
 *
 * One-shot: crc16IbmSdlc(data)
 * Verify:   crc16IbmSdlcSelfTest() returns true on success; run it once on your
 *           toolchain to catch a compiler / endianness mismatch.
 *
 * Generated by crcglot from reveng/crc16-ibm-sdlc -- a verified reference implementation.
 * Reproduce with crcglot:
 *     version:   0.30.0
 *     algorithm: crc16-ibm-sdlc
 *     target:    typescript
 *     variant:   bitwise
 *     comment:   jsdoc
 *     symbol:    crc16_ibm_sdlc
 *     naming:    camel
 */

/** Return the initial CRC state to begin a computation.
 * @returns the starting number state.
 */
export function crc16IbmSdlcInit(): number {
    return 0xFFFF;
}

/** Fold input into the running CRC state and return the new state.
 *
 * Call init -> update (any number of times) -> finalize.
 *
 * @param state - running number state (from init or a prior update).
 * @param data - the message bytes.
 * @returns the updated number state (not yet finalized).
 */
export function crc16IbmSdlcUpdate(state: number, data: Uint8Array): number {
    let crc: number = state;
    for (const byte of data) {
        crc ^= byte;
        for (let j = 0; j < 8; j++) {
            if ((crc & 1) !== 0) {
                crc = (crc >>> 1) ^ 0x8408;
            } else {
                crc >>>= 1;
            }
        }
    }
    return crc;
}

/** Apply the final XOR to produce the CRC.
 *
 * Do not feed the finalized value back into update.
 *
 * @param state - accumulated number state from update.
 * @returns the finished CRC value.
 */
export function crc16IbmSdlcFinalize(state: number): number {
    return state ^ 0xFFFF;
}

/** One-shot convenience: init + a single update + finalize.
 *
 * @param data - the message bytes.
 * @returns the finished CRC value.
 */
export function crc16IbmSdlc(data: Uint8Array): number {
    return crc16IbmSdlcFinalize(crc16IbmSdlcUpdate(crc16IbmSdlcInit(), data));
}

/** Self-test the implementation against independent reference CRCs.
 *
 * Catalogue algorithms check four fixed inputs (the empty string, "123456789", all 256 byte values, and a 1 KiB pattern); the two large inputs are regenerated with a byte-at-a-time loop, so no big array is embedded.  The references come from two independent engines that had to agree.
 *
 * Run once on your target toolchain -- it is the cheapest way to catch a compiler / endianness / width mismatch before trusting the output.
 * @returns true iff the generated CRC reproduces every embedded reference value.
 */
export function crc16IbmSdlcSelfTest(): boolean {
    if (crc16IbmSdlc(new Uint8Array([])) !== 0x0000) return false;
    if (crc16IbmSdlc(new Uint8Array([0x31,0x32,0x33,0x34,0x35,0x36,0x37,0x38,0x39])) !== 0x906E) return false;
    {
        let s = crc16IbmSdlcInit();
        for (let i = 0; i < 256; i++) {
            s = crc16IbmSdlcUpdate(s, new Uint8Array([i]));
        }
        if (crc16IbmSdlcFinalize(s) !== 0x303C) return false;
    }
    {
        let s = crc16IbmSdlcInit();
        for (let i = 0; i < 1024; i++) {
            s = crc16IbmSdlcUpdate(s, new Uint8Array([(i * 167 + 13) & 0xFF]));
        }
        if (crc16IbmSdlcFinalize(s) !== 0x8643) return false;
    }
    return true;
}
```

</details>
